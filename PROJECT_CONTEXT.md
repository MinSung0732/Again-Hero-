# PROJECT_CONTEXT.md

> **새 개발 채팅/AI 세션은 이 파일을 가장 먼저 읽는다.**
> 이 저장소의 프로젝트는 모바일 게임 **「용사, 또 너야? / Again, Hero?」** 이다.

## 1. 프로젝트 한 줄 정의

뱀서라이크를 반대로 뒤집은 모바일 로그라이트 전략 게임.

일반 뱀서라이크에서는 플레이어가 영웅이 되어 몰려오는 몬스터를 처치하지만,
이 게임에서는 **AI 용사가 뱀서라이크 플레이어 역할**을 하고
실제 플레이어는 **마왕이 되어 몬스터를 배치하는 역할**을 맡는다.

AI 용사는 몬스터를 처치하고 경험치를 얻으며 빌드를 스스로 선택한다.
플레이어는 여러 번 실패하면서 용사의 행동/빌드 성향을 파악하고,
몬스터 연구와 영구 성장을 통해 결국 용사를 쓰러뜨린다.

## 2. 핵심 문장

**용사는 성장하고, 마왕은 학습한다.**

게임의 핵심 재미는 단순 소환이 아니라 다음 루프다.

**관찰 → 유도 → 카운터 → 실패/분석 → 연구 → 재도전**

## 3. 반드시 유지할 핵심 컨셉

1. AI 용사는 실제 플레이어처럼 레벨업하고 빌드를 선택한다.
2. AI는 플레이어가 보내는 몬스터 조합에 반응한다.
3. 하지만 AI는 완벽하게 즉시 대응하지 않는다.
   - 최근 전투 데이터 기반 판단
   - 기존 빌드에 대한 관성
   - 용사별 성격/선호도
4. 플레이어는 이 특성을 역이용해 **미끼 공세로 AI 빌드를 유도**할 수 있다.
5. 몬스터는 단순 스탯 덩어리가 아니라 역할과 시너지를 가진다.
6. 패배 시에도 연구 데이터와 메타 진행이 남는다.
7. 영구 성장은 단순 공격력 +N%보다 **새 전략/새 몬스터/정보 해금**을 우선한다.
8. 최종 목표는 한 Run에서 AI 용사를 처치하는 것이다.
9. 장기 진행은 스테이지/챕터 구조로 구성하며, 스테이지마다 다른 용사 또는 다른 용사 세팅을 상대한다.
10. 메타 성장이 쌓이면 과거 스테이지의 용사는 점점 쉽게 압도할 수 있어야 하고, 새로운 스테이지가 다음 난이도 벽이 된다.

## 4. 테마 / 톤

- 한국어 제목: **용사, 또 너야?**
- 영문 제목: **Again, Hero?**
- 시점: 마왕 측
- 분위기: 판타지 + 블랙코미디 + 반복되는 집착/연구
- 마왕은 매번 침입하는 용사를 상대하며 점점 공략법을 연구한다.
- “또 왔냐”라는 반복감이 제목과 세계관의 중심 정서다.

## 5. 플랫폼 / 기술 방향

- Engine: Godot 4.x
- Language: GDScript
- Target: Android 우선, 이후 iOS 고려
- Mobile orientation: **Portrait(세로) 전용**
- Base viewport: **1080 x 1920**
- 가로 회전은 지원하지 않는다.
- 초기 그래픽: 플레이스홀더로 진행
- 초기 목표: 완성품보다 핵심 재미 검증

## 6. 첫 프로토타입 범위

### 용사
1명: 원거리 카이팅형 테스트 용사

필수 기능:
- 자동 이동
- 가장 가까운 적 탐색
- 몬스터와 거리를 벌리고 회피하는 이동
- 사거리 안에서 자동 원거리 투사체 공격
- 투사체 속도 / 최대 이동 사거리
- HP
- 경험치
- 레벨업
- 3개 후보 중 AI가 빌드 선택
- 현재 빌드 UI 표시

현재 기본 용사는 원거리형으로 고정한다.
- 현재 프로토타입 기본 공격 사거리: 430
- 현재 프로토타입 투사체 속도: 680
- 가까운 몬스터에게서 거리를 벌리고, 안전 거리에서는 측면 이동하며 자동 사격
- 투사체는 즉시 적중이 아니라 실제로 이동하며, 최대 사거리를 넘으면 소멸
- **근접 공격 용사는 추후 별도 용사 아키타입으로 추가**한다.

### 몬스터
초기 3종부터 시작:

- 슬라임: 저비용 물량형
- 거미: 저피해 + 둔화
- 오크: 고체력 + 근접 압박

이후 검증 단계에서:
- 폭탄쥐: 사망 시 폭발
- 고블린 궁수: 원거리
- 주술사: 주변 버프

### 경험치 구슬 시스템
- 몬스터는 사망 시 자신의 EXP 보상값을 가진 구슬을 드롭한다.
- Slime 25 / Spider 30 / Orc 40의 기존 EXP 보상값을 그대로 구슬 가치로 사용한다.
- Hero 기본 획득 범위는 150px.
- 범위 밖 구슬은 바닥에 정지.
- 범위 안에 들어온 구슬은 Hero 쪽으로 빨려오며, 가까이 닿으면 EXP가 실제로 증가한다.
- 획득 범위는 Hero 스탯으로 분리해 이후 증강/장비/용사별 특성으로 확장 가능하게 한다.

### 월드 / 카메라 구조
- 전투 맵 전체를 한 화면에 표시하지 않는다.
- 각 Stage는 유한한 최대 맵 크기를 가진다.
- Stage 1 맵: **3200×3200**
- Stage 2 맵: **3600×3600**
- Hero에 Camera2D를 붙여 Hero의 이동을 따라간다.
- 카메라는 맵 경계를 넘지 않는다.
- 상/하단 HUD는 CanvasLayer로 분리해 카메라와 함께 움직이지 않는다.
- 전투 월드는 **상단 HUD 아래~하단 HUD 위**의 전용 SubViewport 안에서만 렌더링해 HUD가 전투 화면을 덮지 않도록 한다.
- 자동 배치는 Hero 현재 위치 기준 약 560~720px 외곽에 몬스터를 생성한다.
- 수동 배치는 현재 카메라 화면에서 터치한 좌표를 실제 월드 좌표로 변환해 배치한다.
- Hero/몬스터/경험치 구슬은 월드 안에 존재하며 카메라 이동에 따라 화면에서 이동해 보인다.

### 플레이어
- 지휘력 자원
- 몬스터 소환 카드
- 스폰 지점 선택
- 지휘력 자동 회복
- 용사 빌드 관찰 UI

## 7. AI 빌드의 최초 설계

AI는 완전한 머신러닝이 아니라 **설명 가능한 점수 기반 Utility AI**부터 구현한다.

예시:

각 레벨업 후보 스킬에 점수를 부여한다.

score =
- 기본 선호도
- 최근 몬스터 개체 수 대응 점수
- 최근 받은 피해 타입 대응 점수
- 현재 빌드 시너지
- 용사 성격 보정
- 약간의 랜덤값

예:
- 적이 많다 → 광역 공격 점수 상승
- 강한 단일 적 비중이 높다 → 단일 화력 점수 상승
- 둔화가 많이 걸린다 → 이동속도/상태저항 점수 상승
- 이미 화염 스킬 Lv.4 → 화염 스킬 후속 강화에 관성 보너스

**중요:** AI는 항상 정답을 고르면 안 된다.
플레이어가 AI를 읽고 유도할 여지가 있어야 한다.

## 8. 첫 번째 재미 검증 시나리오

목표 플레이 경험:

1. 플레이어가 값싼 슬라임을 대량으로 보낸다.
2. AI 용사가 “다수의 적” 상황을 인식한다.
3. 용사가 광역 스킬에 투자한다.
4. 플레이어가 이를 확인하고 몬스터 구성을 바꾼다.
5. 고체력 오크 + 둔화 거미처럼 광역기에 덜 취약한 조합으로 전환한다.
6. 용사는 이미 광역기에 투자한 탓에 즉시 완벽 대응하지 못한다.
7. 플레이어가 “내가 용사의 빌드를 유도했다”는 감각을 얻는다.

이 경험이 재미없다면 콘텐츠를 늘리기 전에 핵심 구조부터 수정한다.

## 9. 전투 자원 기본값 (임시)

밸런스 확정값이 아니라 프로토타입용 시작점이다.

- 최대 지휘력: 100
- 지휘력 회복: 초당 3
- 슬라임 비용: 3
- 거미 비용: 7
- 오크 비용: 18
- Run 목표 시간: 초기 프로토타입 5~10분

수치는 테스트 후 변경한다.

## 10. Run 내 마왕 증강

마왕(실제 플레이어)도 한 Run 안에서 성장한다.

- 특정 전황/기믹 시점에 증강 선택 발생
- 3개 후보 중 1개 선택
- 후보 새로고침은 **Run당 총 3회 공유**
- 새로고침 시 3개 후보 전체 교체
- 직전 버린 후보의 즉시 재등장은 방지하는 방향
- 마왕 증강은 Run 종료 시 초기화
- 영구 연구/내실과 별개 시스템
- 단순 공격력 +N%보다 몬스터 행동, 지휘력, 조합, AI 유도 전략을 바꾸는 증강을 우선

예시 방향:
- 분열 슬라임
- 시체 재활용(지휘력 환급)
- 오크 돌격 강화
- 미끼 공세 비용 감소
- 특정 몬스터 역할 변화

용사는 레벨업마다 AI가 증강을 선택하고,
마왕은 특정 전황마다 플레이어가 증강을 선택하는 **비대칭 성장 구조**를 목표로 한다.

## 11. 스테이지 / 용사 진행 구조

게임의 장기 진행은 **스테이지(챕터) 단위**로 구성한다.

핵심 원칙:
- 플레이어는 스테이지를 순서대로 클리어하며 진행한다.
- 각 스테이지는 특정 용사 또는 특정 용사 아키타입/세팅을 상대한다.
- 스테이지가 올라갈수록 용사의 기본 스탯, AI 성향, 시작 무기/기믹, 증강 풀 등이 달라진다.
- 초반 스테이지 용사는 의도적으로 단순하고 약하게 설계해 초반 밸런스를 잡기 쉽게 한다.
- 플레이어가 연구/내실을 충분히 쌓은 뒤 과거 스테이지로 돌아가면 초반 용사를 압도할 수 있어야 한다. 이것이 성장 체감과 파밍 보상이 된다.
- 새 스테이지에서는 더 강하거나 다른 대응법을 요구하는 용사가 등장해 다시 전략 연구가 필요해진다.
- 즉, 메타 성장으로 모든 용사가 영구히 같은 난이도로 남는 구조가 아니라 **과거 스테이지는 쉬워지고, 신규 스테이지가 새로운 벽이 되는 구조**를 지향한다.

프로토타입 방향 예시:
- Stage 1: 원거리 카이팅형 초급 용사 — 현재 테스트 Hero
- Stage 2: 연사/기동 특화 원거리 용사
- Stage 3: 고체력 근접 전사형 용사
- 이후: 상태이상 저항형, 광역 특화형, 소환형 등

각 스테이지 데이터에는 최소 다음 항목을 둔다.
- stage_id / 표시 이름
- hero_id
- Hero 기본 스탯 보정
- Hero AI 성격/선호도
- 사용 가능한 Hero 증강 풀
- 전장/런 시간/특수 규칙
- 최초 클리어 보상 및 다음 스테이지 해금 조건

장기적으로는 모바일 챕터형 진행처럼 **현재 최고 스테이지를 공략하고, 막히면 이전 콘텐츠/연구를 통해 내실을 쌓은 뒤 재도전**하는 루프를 목표로 한다.

## 12. 프로젝트 구조 방향

```text
res://
├─ project.godot
├─ README.md
├─ PROJECT_CONTEXT.md
├─ AGENTS.md
├─ docs/
│  ├─ GDD.md
│  └─ ROADMAP.md
├─ src/
│  ├─ main/
│  ├─ battle/
│  ├─ hero/
│  ├─ monsters/
│  ├─ ai/
│  ├─ systems/
│  ├─ ui/
│  └─ data/
└─ assets/
   ├─ art/
   ├─ audio/
   └─ fonts/
```

폴더는 실제 기능이 생길 때 파일을 추가한다. 빈 폴더 유지를 위한 불필요한 파일은 만들지 않는다.

## 13. 코드 설계 원칙

- 데이터와 동작을 가능한 한 분리한다.
- 몬스터/스킬 수치를 코드 곳곳에 하드코딩하지 않는다.
- 향후 Resource 기반 데이터 정의를 사용할 수 있도록 설계한다.
- Hero, Monster, BattleManager, SpawnManager, BuildAI 책임을 분리한다.
- 초기에 과도한 추상화/ECS 도입은 하지 않는다.
- 프로토타입에서는 읽기 쉽고 수정하기 쉬운 코드를 우선한다.
- 모바일이므로 매 프레임 모든 몬스터가 전체 탐색하는 구조는 피한다.
- 다수 몬스터를 전제로 성능을 관찰한다.

## 14. 현재 개발 단계

**Phase 4.5 진입 — Stage / Hero 데이터화 v1**

현재 구현됨:
- Godot Android Editor에서 프로젝트 열기 확인
- Battle 씬
- 원거리 Hero 자동 이동/가장 가까운 몬스터 탐색/자동 투사체 공격
- Slime 추적/공격
- HP / 데미지 / 사망
- Hero HP 및 남은 Slime 수 HUD
- 전투 종료 결과 화면
- "다시 실험하기" 재시작 버튼
- 외부 아트 없이 GDScript draw 함수로 테스트용 캐릭터 표시

기존의 자동 혼합 테스트 웨이브는 제거했다.
현재는 Hero 1명이 중앙에서 시작하고, 플레이어(마왕)가 하단 모바일 UI에서 Slime / Spider / Orc를 직접 소환한다.
전장에 몬스터가 0마리여도 Run은 종료되지 않으며, 지휘력이 회복되면 다시 공세를 보낼 수 있다.
용사가 죽으면 플레이어(마왕) 승리로 처리한다.

Android 실기기에서 Milestone 1 실행 및 기본 전투 흐름이 정상 작동함을 확인했다. 이후 프로젝트 방향을 **세로형 전용(1080×1920, Portrait lock)** 으로 확정했고, HUD/전장도 세로 레이아웃으로 전환했다. 이제 Milestone 2(Hero EXP / 레벨업)로 이동한다.

현재 추가 구현됨:
- 몬스터 처치 시 즉시 EXP를 주지 않고 **경험치 구슬을 사망 위치에 드롭**
- Hero가 기본 획득 범위 안으로 접근하면 경험치 구슬이 Hero에게 끌려오고, 접촉 시 EXP 획득
- Hero 기본 경험치 획득 범위: **150px**
- 경험치 구슬은 한번 자석화되면 Hero가 이동해도 계속 추적
- Hero Level / EXP / 다음 레벨 필요 EXP
- 상단 HUD의 Lv / EXP Bar
- 용사 레벨업마다 증강 후보 3개 생성
- Hero 증강 데이터 8종
  - 탄환 강화: 투사체 공격력 증가
  - 연사 강화: 공격속도 증가
  - 강인한 육체: 최대 HP/회복
  - 민첩한 발놀림: 이동속도 증가
  - 둔화 적응: 둔화 저항 증가
  - 사거리 확장: 공격/투사체 사거리 증가
  - 폭발 탄환: 광역 피해 반경/비율 증가
  - 전투 회복: 즉시 회복
- Hero 증강 최대 중첩은 카탈로그의 `max_stack`으로 제한:
  - 탄환 강화 5 / 연사 강화 5 / 강인한 육체 5 / 민첩한 발놀림 2
  - 둔화 적응 3 / 사거리 확장 3 / 폭발 탄환 4 / 전투 회복 3
- Build AI v1이 기본 선호도 + 주변 적 수 + 현재 HP + 가장 가까운 적 거리 + 기존 빌드 관성 + 작은 랜덤값을 종합해 1개 자동 선택
- 기본 Hero를 근접 자동공격에서 **원거리 투사체 + 카이팅 회피 이동**으로 전환
- 원거리 Hero 초기값: 사거리 430 / 투사체 속도 680 / 공격 주기 0.62초
- 몬스터가 없을 때도 Hero는 멈추지 않는다.
- **몬스터가 없고 맵에 EXP 구슬이 남아 있으면 가장 가까운 EXP 구슬을 우선 회수하러 이동**한다.
- 몬스터도 EXP 구슬도 없을 때만 전장 내부의 임의 목표 지점으로 배회한다.
- 몬스터가 나타나면 즉시 카이팅/공격 행동으로 전환
- 선택된 증강이 실제 Hero 스탯에 즉시 적용
- 하단 HUD에 후보 / AI 선택 / 현재 용사 빌드 표시

현재 Build AI v1 판단 요소:
- 주변 320px 내 몬스터 수
- 전체 생존 몬스터 수
- 현재 HP 비율
- 가장 가까운 몬스터 거리
- 슬라임 / 거미 / 오크 타입 비율
- 물량형(swarm) / 제어형(controller) / 탱커형(tank) 역할 비율
- 동일 증강 기존 중첩 수에 따른 빌드 관성
- 작은 랜덤값

현재 몬스터 프로토타입:
- 슬라임: 물량형, HP 60, EXP 25
- 거미: 제어형, 저체력/고기동, 공격 시 용사 이동속도를 일시적으로 72%로 둔화, EXP 30
- 오크: 탱커형, HP 140, 느리지만 강한 근접 공격, EXP 40

### Stage 1 게임레디 스프라이트시트 v2
- 사용자가 제공한 `stage1_mage_game_ready.zip` 기준으로 애니메이션 구조를 다시 정리.
- 셀 크기 64×64, 6열×4행 스프라이트시트 사용.
- 실제 연결 프레임:
  - idle: 4프레임 / 5.5 FPS / loop
  - move: 6프레임 / 기본 10 FPS / 이동속도에 따라 약 0.72~1.35배 재생속도
  - attack: 6프레임 / 18 FPS / 1회 재생
  - hit: 3프레임 / 14 FPS / 1회 재생
- Hero renderer를 `Sprite2D`에서 `AnimatedSprite2D`로 변경.
- 이동 중 한 프레임이 미끄러지던 방식 제거.
- 기존 Stage 1 Base64 PNG / pose-data / RLE 런타임 복원 방식은 완전히 제거.
- 스프라이트시트 경로: `res://assets/art/heroes/stage1_mage/stage1_mage_spritesheet.png`
- 바이너리 아트 설치는 Base64를 사용하지 않는 `tools/install_stage1_mage_art.sh`로 지원.
- 제공된 게임레디 팩에는 death 전용 프레임이 없으므로 현재 사망은 hit 애니메이션 후 짧은 fade/tilt 처리. 추후 death 프레임이 제공되면 교체.

### Stage 1 도트 상태 애니메이션 v1
- 사용자가 제공한 Stage 1 마법사 시트의 포즈를 게임 상태와 연결.
- 별도 바이너리 에셋 의존을 줄이기 위해 32×32 포즈를 RLE + 팔레트 텍스트 데이터로 저장하고 런타임에 ImageTexture로 복원.
- 대기: 2개 포즈를 약 0.42초 간격으로 전환.
- 걷기: 이동 시 walk 포즈 + 가벼운 상하 보빙.
- 달리기/카이팅: 빠른 이동 시 run 포즈 + 더 빠른 보빙.
- 공격: 투사체 발사 직후 약 0.24초 attack 포즈.
- 피격: 피해 직후 약 0.20초 hit 포즈.
- 사망: death_0 → death_1 순서로 표시한 뒤 약 0.68초 후 Hero 사망 신호/Stage Clear 처리.
- 이동 방향이 왼쪽이면 Sprite를 좌우 반전.
- 현재 v1은 대표 포즈 중심이며, 이후 원본 시트의 중간 프레임을 더 넣어 프레임 수를 확장할 수 있다.

### Stage 1 비주얼
- 사용자가 제공한 Stage 1 마법사 도트 시안을 기반으로 실제 전투 Sprite를 적용.
- **Stage 1 도트 적용 안정화 v2:** Android에서 SVG preload 실패가 Hero script 전체를 막는 문제가 확인되어 SVG 직접 preload를 제거. 사용자가 제공한 32×32 PNG를 런타임에 안전하게 디코딩해 표시하며, 실패해도 플레이스홀더 Hero/전투 로직은 유지된다.
- 현재는 32×32 정지 프레임 1장을 3배 스케일로 표시하는 v1.
- Stage 1 `견습 마도사` profile에만 sprite_path를 연결.
- Stage 2 기동 사냥꾼은 아직 플레이스홀더 비주얼을 사용.
- 추후 걷기/공격/피격/사망 프레임을 AnimatedSprite2D로 확장 예정.

현재 Stage 선택 / 연구 포인트 v1:
- 상단 `스테이지` 버튼으로 전투 중 Stage 선택 메뉴를 열 수 있다.
- Stage 메뉴를 열면 전투/지휘력 회복이 일시 정지한다.
- 결과창에서도 `스테이지 선택` 버튼으로 메뉴 진입 가능.
- 해금된 Stage는 언제든 선택해 재도전 가능.
- Stage 버튼에 용사 이름 / 해금·클리어 상태 / 최초 클리어 보상을 표시.
- 영구 메타 자원 **연구 포인트** 추가.
- Stage 1 최초 보상: 연구 포인트 100
- Stage 2 최초 보상: 연구 포인트 150
- 연구 포인트와 보상 획득 여부는 `user://stage_progress.cfg`에 영구 저장.
- 기존 저장에서 이미 Stage를 클리어했더라도 reward_claimed 기록이 없다면 다음 클리어 때 최초 보상을 1회 받을 수 있다.
- 연구 포인트 사용처로 **영구 연구/내실 v1**이 구현되었다.
- 연구 메뉴에서 연구 포인트를 소비하고, 연구 레벨은 `user://stage_progress.cfg`에 영구 저장된다.
- 구매 효과는 현재 진행 중인 전투에 즉시 끼워 넣지 않고 **다음 Run 시작 시 적용**된다.
- 현재 연구 5종:
  - 마력 저장고: 최대 지휘력 +10 / Lv, 최대 Lv.3
  - 마력 순환: 지휘력 회복 +0.25/초 / Lv, 최대 Lv.3
  - 슬라임 배양 최적화: Slime 소환 비용 -5% / Lv, 최대 Lv.3
  - 전술 기록 노트: Run당 마왕 증강 리롤 +1 / Lv, 최대 Lv.3
  - 고속 실험법: 몬스터 소환 시 마왕 EXP 획득 +5% / Lv, 최대 Lv.3
- 연구 비용은 `src/data/research_catalog.gd`에서 데이터로 관리한다.

현재 Stage / Hero 데이터 v1:
- `src/data/stage_catalog.gd`에서 Stage 데이터를 관리
- `src/data/hero_profiles.gd`에서 Hero 기본 스탯/아키타입 데이터를 관리
- 현재 플레이 Stage는 `stage_1`
- Stage 1: **첫 번째 침입자**
  - Hero: **견습 마도사**
  - archetype: ranged_kiter
  - HP 300 / 이동 230 / 공격 34 / 사거리 430 / 공격주기 0.62 / 투사체속도 680 / EXP 획득범위 150
- Stage 2: **빠른 사냥꾼**
  - Hero: **기동 사냥꾼**
  - Stage 1 최초 클리어 후 해금
  - 결과창의 다음 스테이지 버튼으로 진입 가능
  - 현재 Stage와 최고 해금 Stage는 `user://stage_progress.cfg`에 저장
- Battle은 Stage의 hero_id를 읽고 해당 Hero profile을 적용해 Hero를 생성
- 상단 HUD에 Stage 번호 / Stage 이름 / Hero 이름 표시
- Hero 처치 시 결과창을 `STAGE CLEAR`로 표시

현재 마왕 성장 / 증강 v2:
- 마왕도 Hero처럼 **Lv / EXP**를 가진다.
- 몬스터를 소환할 때마다 **실제로 소비한 소환 비용만큼 마왕 EXP 획득**.
  - 예: Slime 비용 3.0 → 마왕 EXP +3.0
  - 소환 비용 감소 증강이 적용되면 감소된 실제 비용만큼 EXP를 얻는다.
- 마왕 초기 Lv.1, 첫 레벨업 필요 EXP 30.
- 이후 필요 EXP는 레벨당 +15씩 증가한다.
- 마왕 Lv.1은 몬스터 기본 스탯 ×1.00 기준이며, Lv.2부터 레벨 상승마다 몬스터 HP/공격력 ×1.05, 이동속도 ×1.02를 누적 적용한다.
- 이동속도 레벨 배율은 최대 ×1.25로 제한한다.
- 마왕 레벨 상승 시 이미 살아있는 몬스터에도 즉시 재계산하며, HP는 현재 HP 비율을 유지한다.
- **마왕 레벨업 시마다 증강 후보 3개 중 1개를 직접 선택**.
- 새로고침은 **Run 전체 공유 3회**.
- 새로고침 직후 방금 버린 후보는 즉시 다시 나오지 않도록 제외.
- 증강 선택 중에는 Hero / 몬스터 / EXP 구슬 / 투사체 전투 진행이 잠시 정지.
- 현재 증강 9종은 마왕 레벨 기본 스탯 성장과 과도하게 겹치지 않도록 최대 중첩과 효과량을 조정:
  - 어둠의 순환: 지휘력 회복 +0.45/초, 최대 4중첩
  - 전쟁 경제: 소환 비용 -10%, 최대 3중첩
  - 군단 훈련: 새 몬스터 이동속도 +7%, 최대 2중첩
  - 시체 재활용: 사망 시 실제 소환 비용 환급 +12%p, 최대 3중첩(36%)
  - 분열 실험: Slime 사망 무료 Slime 생성 확률 +15%p, 최대 3중첩(45%)
  - 독거미 둥지: 새 Spider 둔화 지속시간 +20%, 최대 2중첩
  - 오크 중장갑: 새 Orc 최대 HP +12%, 최대 3중첩
  - 잔혹한 명령: 새 몬스터 공격력 +6%, 최대 2중첩
  - 마력 비축: 최대/즉시 지휘력 +15, 최대 4중첩
- 순수 HP/공격력/이동속도는 마왕 레벨이 공통 성장축을 담당하고, Run 증강의 순수 스탯 증가는 보조적인 폭으로 제한한다.
- 최대 중첩 전까지 같은 마왕 증강이 이후 레벨업 후보에 다시 등장할 수 있으며, 최대 중첩에 도달하면 후보 풀에서 제외한다.
- 마왕 증강 선택 카드에 현재 중첩 → 선택 후 중첩 / 최대 중첩을 표시한다.
- 전투 상단 디버그 표기는 현재 마왕 레벨 배율, 신규 소환 Run 증강 보정, 생존 몬스터 타입별 실제 HP/공격력/이동속도 샘플을 0.25초 간격으로 표시한다.
- 이 시스템은 현재 마왕이 Run 안에서 강해질 수 있는 첫 성장 수단이며, 이후 연구/영구 내실과 분리한다.

현재 마왕 직접 소환 v1:
- 최대 지휘력: 100
- 시작 지휘력: 0
- 자동 회복: 초당 3
- Slime 비용: 3
- Spider 비용: 7
- Orc 비용: 18
- 하단 버튼 터치로 소환
- 지휘력이 부족하면 해당 버튼 비활성화
- **자동 / 수동 배치 스위치** 제공
- 자동 배치: 몬스터 버튼을 누르면 전장 가장자리 후보 중 자동 소환
- 수동 배치: 몬스터 버튼으로 종류를 선택한 뒤 전장을 터치한 위치에 소환
- 수동 배치는 Hero 중심 220px 이내에는 소환할 수 없다. 이 수치는 내부 밸런스 값이며 플레이어 안내문에는 직접 노출하지 않는다.
- Hero 근접 배치 실패 시 터치 위치에 `용사와 너무 가까움`을 표시하고, 동시에 Hero 중심의 실제 금지 반경 원형을 약 0.7초간 보여준 뒤 페이드아웃한다.
- 수동 배치 실패 사유(용사와 너무 가까움 / 전장 밖 / 지휘력 부족)는 하단 로그보다 **사용자가 누른 전장 위치에 데미지 숫자처럼 떠오르는 플로팅 텍스트**로 우선 표시한다.
- 수동 배치에서 선택한 몬스터 종류는 유지되어 연속 배치 가능
- 몬스터 전멸 시에도 전투가 끝나지 않아 다음 공세를 준비할 수 있음
- 실제 플레이어가 만든 몬스터 구성은 기존 Build AI 타입/역할 분석에 그대로 반영됨

아직 완성되지 않은 것:
- 증강 간 시너지 점수
- 영구 연구의 정보/전략 해금 확장
- Run 종료 통계/분석 저장
- 정식 아트/사운드

## 15. 다음 구현 순서

다음 작업은 순서를 크게 바꾸지 않는다.

1. Stage 1에서 6분 Run 실기기 테스트
2. 슬라임 미끼 → 폭발 탄환 유도 → 오크/거미 전환 시 결과 분석 확인
3. 전략 전환 판정 window/threshold 밸런스 조정
4. Hero별 AI 성향/증강 풀 차별화 확장
5. 분석 결과 영구 저장/누적 통계는 핵심 재미 확인 후 확장
4. Run 종료/통계/분석 화면 확장
5. 영구 연구의 정보 해금 / 새로운 전략 옵션 확장

## 16. 새 채팅에서 AI에게 기대하는 작업 방식

새 채팅에서 이 저장소를 작업할 때:

1. 이 파일과 README, docs/ROADMAP.md를 먼저 읽는다.
2. 기존 핵심 컨셉을 임의로 다른 장르로 바꾸지 않는다.
3. 구현 전에 현재 저장소 파일을 확인한다.
4. 가능한 작은 단위로 실제 동작하는 기능을 추가한다.
5. 핵심 재미 검증 전에는 콘텐츠 양을 과도하게 늘리지 않는다.
6. 구현 후 변경 파일과 테스트 방법을 설명한다.
7. 새로운 중요한 결정이 생기면 **PROJECT_CONTEXT.md도 갱신한다.**
8. 기능 추가/패치/개선/제거/밸런스 변경마다 **CHANGELOG.md를 반드시 갱신한다.**

## 17. 현재 가장 중요한 질문

> AI 용사의 선택을 관찰하고, 그 선택을 일부러 유도한 뒤 카운터하는 과정이 실제로 재미있는가?

모든 초기 개발은 이 질문을 검증하는 방향으로 진행한다.


### Stage 1 스프라이트 Android Editor 로딩 안정화
- Termux로 PNG를 설치한 직후 Android Godot Editor의 import/cache가 아직 갱신되지 않으면 `ResourceLoader.exists()`가 false가 되어 기존 도형 Hero로 fallback되는 문제가 확인됨.
- Stage 1 스프라이트시트 로딩은 이제:
  1. Godot imported Texture2D 로드 시도
  2. 실패 시 `FileAccess.file_exists(res://...)` 확인
  3. raw PNG를 `Image.load()`로 직접 읽고 `ImageTexture` 생성
  순서로 처리한다.
- 따라서 파일이 실제 프로젝트 폴더에 있으면 import 캐시가 늦어도 Stage 1 도트가 표시된다.
- Base64 방식은 사용하지 않는다.


### Stage 1 마법사 투사체 애니메이션 v1
- 사용자가 제공한 마법탄 4프레임을 Stage 1 `ranged_rookie / 견습 마도사` 전용 투사체로 연결.
- Base64를 사용하지 않고 PNG 스프라이트시트를 직접 로드.
- 리소스 경로:
  `res://assets/art/projectiles/stage1_mage/stage1_mage_projectile_sheet.png`
- 시트: 128×128 셀 × 4프레임, 총 512×128.
- fly 애니메이션: 4프레임 / 12 FPS / loop.
- 원본이 오른쪽 진행 기준이므로 projectile Area2D 자체를 발사 방향 각도로 회전.
- Stage 1이 아닌 Hero는 아직 기존 원형 테스트 탄환 fallback 사용.
- Android Godot import cache가 늦을 때는 raw PNG를 `Image.load()`로 직접 읽어 표시.
- 충돌/데미지/속도/최대 사거리 로직은 기존 값을 유지하고 비주얼만 교체.
- 에셋 설치 도구: `tools/install_stage1_projectile_art.sh`.


### Stage 1 마법사 투사체 적용 안정화
- Stage 1 마법탄은 오른쪽 진행 기준 4프레임 fly loop를 사용한다.
- 128×128 공통 셀, 12 FPS loop.
- 밝은 마법 코어를 실제 projectile origin/collision 중심에 맞추기 위해 sprite offset을 사용한다.
- **투사체 적용 순서 버그 수정:** `source_hero_id`는 Hero가 projectile을 생성한 뒤 `setup()`에서 전달되므로, 전용 비주얼 적용도 `setup()` 이후에 실행해야 한다.
- 이 순서를 지켜 Stage 1에서는 전용 AnimatedSprite2D가 뜨고, 다른 Hero/에셋 누락 시에만 기존 원형 fallback을 사용한다.
- Base64 리소스 방식은 사용하지 않는다.


### Stage 1 투사체 Android 로딩 안정화 v2
- 합본 시트를 128×128 AtlasTexture로 잘라 쓰던 경로에서 Android Editor 환경의 로딩 실패가 발생해 노란 fallback 탄이 보일 수 있었음.
- 현재는 `projectile_01.png` ~ `projectile_04.png` **개별 PNG 4장을 직접 읽는 방식**으로 변경.
- imported texture를 우선 사용하고, import cache가 아직 갱신되지 않았으면 `FileAccess` + `Image.load_png_from_buffer()`로 raw PNG를 직접 읽는다.
- Base64는 사용하지 않는다.


### 영구 연구 / 내실 시스템 v1
- `src/data/research_catalog.gd`에 연구 데이터/비용을 분리.
- `StageProgress`가 연구 레벨과 연구 포인트 소비를 영구 저장.
- Stage 선택 메뉴에서 `마왕 연구` 화면으로 진입.
- 연구 포인트가 부족한 항목은 구매 비활성화, 최대 레벨 도달 시 연구 완료 표시.
- 연구 효과는 Battle 시작 시 저장된 연구 레벨을 읽어 적용.
- 시작 지휘력 0 규칙은 유지한다.
- Run 내 마왕 증강과 영구 연구는 별개의 성장축으로 유지한다.


### Hero AI 최근 공세 기억 v1
- Hero는 레벨업 순간의 현재 화면만 보지 않고 최근 20초 동안 플레이어가 직접 소환한 몬스터 공세를 기억한다.
- 기억은 시간에 따라 가중치가 감소하며 20초가 지나면 제거된다.
- 현재 전장 type/role 비율 + 최근 공세 type/role 비율을 함께 Hero 증강 Utility 점수에 반영한다.
- 목적은 플레이어가 미끼 공세를 반복해 Hero의 다음 빌드를 유도한 뒤 다른 공세로 카운터할 수 있게 하는 것.


### 데이터 중심 설계 원칙
- 앞으로 콘텐츠 업데이트가 계속될 것을 전제로, 콘텐츠 값과 런타임 로직을 분리한다.
- `src/data/monster_catalog.gd`: 몬스터 ID / 표시명 / 역할 / 기본 소환 비용 / Scene 관리.
- `src/data/hero_augment_catalog.gd`: Hero 증강 기본 정보뿐 아니라 AI 평가 규칙(`ai_rules`)까지 관리.
- `src/ai/hero_build_ai.gd`: 특정 Slime/Spider/Orc 또는 특정 증강 ID를 직접 분기하지 않고 공통 rule evaluator로 점수를 계산.
- Hero의 현재 전황 및 최근 공세 기록은 고정된 세 종류 배열이 아니라 동적 Dictionary로 집계한다.
- 새 몬스터가 Catalog에 추가되고 자신의 `monster_type / monster_role`을 제공하면 현재/최근 공세 분석 구조가 별도 코드 수정 없이 해당 키를 집계할 수 있다.
- 프로토타입 속도를 해치는 과도한 범용 프레임워크는 만들지 않되, 반복될 가능성이 높은 콘텐츠 정의는 Catalog 중심으로 확장한다.


### Hero Missing After Data-Driven AI Refactor — Fix
- 데이터 중심 AI 리팩터링 과정에서 삭제한 `_role_for_monster_type()` 함수 참조가 최근 공세 메모리 코드에 남아 Hero script 로드가 실패하는 문제가 발생.
- 해당 fallback을 `MONSTER_CATALOG.get_role(monster_type)`로 교체해 복구.
- 이후 리팩터링 시 삭제된 helper의 잔여 참조를 반드시 확인한다.


### Hero AI 판단 지연 / 빌드 관성 v1
- Hero는 레벨업 순간의 실시간 전장을 그대로 읽지 않고 **주기적으로 저장한 관측 스냅샷**으로 증강을 판단한다.
- 따라서 플레이어가 공세를 급격히 바꿔도 Hero가 즉시 완벽하게 대응하지 않는다.
- 관측/관성 수치는 `hero_profiles.gd > ai_settings`에서 Hero별 데이터로 관리한다.
- Stage 1 견습 마도사:
  - observation_interval: 4.0초
  - stack_inertia: 1.25
  - new_branch_penalty: 0.70
  - 비교적 느리게 적응하고 이미 선택한 빌드를 더 고집하는 성향.
- Stage 2 기동 사냥꾼:
  - observation_interval: 2.8초
  - stack_inertia: 0.95
  - new_branch_penalty: 0.40
  - Stage 1보다 빠르게 적응하고 빌드 전환 저항이 약한 성향.
- 기존 증강을 다시 선택하면 누적 stack 수에 따라 Utility 보너스를 받는다.
- 이미 빌드가 있는 상태에서 전혀 새로운 증강을 고르면 Hero profile의 `new_branch_penalty`만큼 Utility가 감소한다.
- AI 선택 이유 UI에 사용한 관측 정보가 몇 초 전 데이터인지 표시한다.
- 이 구조는 특정 증강 ID에 의존하지 않고 공통 계산으로 적용된다.


### Hero 증강 시너지 v1
- Hero AI가 전황/최근 공세/관성뿐 아니라 **현재 빌드와 후보 증강의 시너지**도 Utility 점수에 반영한다.
- 시너지 로직은 특정 증강 ID를 코드에서 분기하지 않고 `hero_augment_catalog.gd` 데이터로 관리한다.
- 각 증강은 `tags`와 `synergy_rules`를 가질 수 있다.
- 현재 태그 예:
  - damage / projectile / attack_speed
  - durability / survival / recovery
  - mobility / kite / range
- 현재 조합 예:
  - 탄환 강화 ↔ 연사 강화
  - 민첩한 발놀림 ↔ 사거리 확장
  - 강인한 육체 ↔ 전투 회복
- `HeroAugmentCatalog.get_build_tag_counts()`가 현재 build_counts를 태그 스택으로 변환한다.
- `HeroBuildAI`는 `build_tag_stacks / build_tag_present / build_augment_stacks / build_augment_present` 공통 synergy rule을 평가할 수 있다.
- 시너지 보너스는 기존 전황 점수, 최근 공세 점수, 빌드 관성, 새 갈래 전환 저항과 함께 합산된다.
- 선택 이유 UI에도 가장 큰 시너지 근거와 보너스 점수를 표시한다.
- 새 증강을 추가할 때 AI 코드를 수정하지 않고 tags/synergy_rules 데이터만 추가하는 방향을 유지한다.


### Hero 광역 대응 / 미끼 카운터 기반 v1
- Hero 증강에 **폭발 탄환** 추가.
- 투사체 적중 시 직접 대상은 기존 전체 피해를 받고, 주변 몬스터는 현재 splash 비율만큼 광역 피해를 받는다.
- 폭발 탄환은 반복 선택 시:
  - projectile_splash_radius +70 / stack, 최대 180
  - projectile_splash_damage_ratio +0.28 / stack, 최대 0.70
- AI는 현재/최근 Slime·swarm 비중, 근처 적 수, 최근 소환 횟수가 높을수록 폭발 탄환을 선호한다.
- Orc/tank 비중은 기존 projectile_power(탄환 강화) 쪽 Utility를 높이므로 물량 미끼 후 고체력 단일 압박으로 전환하는 카운터 실험이 가능해졌다.
- 폭발 탄환 투자는 단일 대상 직접 피해를 올리지 않기 때문에, Slime을 미끼로 광역 빌드를 유도한 뒤 Orc 중심으로 전환하면 투자 기회비용이 발생한다.
- Hero 증강 효과 적용도 `hero.gd`의 증강 ID별 match에서 제거하고 `hero_augment_catalog.gd > effects` 데이터 기반 공통 적용기로 전환.
- 현재 공통 effect op:
  - add_stat
  - multiply_stat
  - heal
- 새 증강은 가능한 한 `effects / ai_rules / tags / synergy_rules` 데이터만 추가해 확장한다.


### 실제 상태이상 기억 / 둔화 저항 v1
- Hero AI가 Spider가 존재했다는 사실만 보는 것이 아니라 **실제로 Hero에게 들어온 상태이상 적중 기록**을 기억한다.
- 상태이상 기록은 최근 20초 시간창으로 유지되며 시간이 지나면 가중치가 감소한다.
- `status_effect_catalog.gd` 추가:
  - 현재 slow(둔화) 등록
  - 이후 poison / burn / silence 등 추가 가능
- Hero context에 최근 상태이상:
  - event count
  - status별 실제 적중 횟수
  - 시간 감쇠 weight
  를 포함한다.
- Build AI에 범용 `recent_status_weight` rule 추가.
- 기존 **민첩한 발놀림**도 실제 둔화 적중이 많을수록 점수가 추가 상승한다.
- Hero 신규 증강 **둔화 적응** 추가:
  - 둔화 저항 +18% / stack
  - 최대 65%
  - 저항 수치만큼 둔화 강도와 지속시간을 모두 완화
- 상태 저항은 `status_resistances` Dictionary로 관리하며 slow 전용 하드코딩 스탯을 만들지 않는다.
- 증강 effect op에 `add_status_resistance` 추가.
- AI 선택 이유에 `최근 20초 둔화 4회`처럼 실제 전투 적중 기록을 표시할 수 있다.


### Run 시간 / 전략 전환 분석 v1
- Milestone 5 핵심 재미를 실제 한 판 단위로 검증하기 위한 Run 제한시간/분석 시스템을 추가.
- Stage 데이터에 `run_duration_seconds` 추가:
  - Stage 1: 360초(6분)
  - Stage 2: 420초(7분)
- 제한시간 동안 Hero를 처치하지 못하면 **시간 초과 패배**.
- 스테이지 메뉴, 연구 메뉴, 마왕 증강 선택처럼 전투가 일시정지된 동안에는 Run 시간이 흐르지 않는다.
- 상단 HUD에 남은 시간을 MM:SS로 표시.
- `src/systems/run_metrics.gd` 추가. Battle에서 분석 책임을 분리.
- 현재 기록:
  - 직접 소환 몬스터 수 / 지휘력 소비량
  - 몬스터 사망 횟수
  - Hero 최저 HP
  - Hero 증강 선택 시점
  - 전략 전환 시점
- 전략 전환 판정:
  - 최근 15초 직접 소환 기록 사용
  - 단순 소환 횟수가 아니라 **실제 지휘력 소비량**을 weight로 사용
  - 최근 소비 12 이상 + 한 몬스터가 60% 이상이면 현재 주력 공세로 인정
  - 주력 공세가 다른 타입으로 바뀌면 `슬라임 → 오크` 형태로 시각을 기록
  - 6초 전환 cooldown으로 순간적인 버튼 섞임에 의한 노이즈를 완화
- Run 종료 결과창에 Run 시간 / Hero 최저 HP / 직접 소환 구성 / 전략 전환 시점 / Hero 증강 횟수와 마지막 선택을 표시.
- 이 수치는 프로토타입 검증용이며 실기기 5~10분 플레이 후 threshold/window를 조정한다.

### Hero 피격 무적시간 / 다중타격 보호
- Hero는 피해를 실제로 받은 뒤 짧은 피격 무적시간을 가진다.
- 현재 Stage 1/2 Hero 기본값은 0.35초이며 `hero_profiles.gd > invulnerability_duration`에서 Hero별로 조절한다.
- 무적시간 동안 들어오는 추가 `take_damage()` 호출은 피해를 적용하지 않는다.
- 여러 몬스터가 Hero와 겹치거나 공격 타이밍이 동시에 맞아도 한 순간에 여러 번 피해가 중첩되지 않는다.
- 무적 중 Hero는 빠르게 깜빡여 현재 추가 피해를 받지 않는 상태임을 시각적으로 보여준다.
- Spider의 slow는 실제 피해가 성공한 공격에서만 적용해 무적시간에 막힌 공격이 상태이상만 남기지 않게 한다.

### Hero 방향 전환 안정화 / 아틀라스 경계 처리
- Hero 비주얼은 이동 수평속도 부호가 한 프레임 바뀌었다고 즉시 좌우반전하지 않는다.
- Stage 1 견습 마도사는 반대 방향이 약 0.14초 유지될 때 방향을 바꾸며, 작은 수평 흔들림은 무시한다.
- 공격 시작 시 실제 투사체 발사 방향으로 즉시 바라보고, 공격 모션 동안에는 방향을 고정한다.
- 방향 전환 지연과 최소 수평속도는 Hero profile 데이터로 관리한다.
- Stage 1 스프라이트시트는 64×64 셀이 패딩 없이 붙어 있으므로 AtlasTexture `filter_clip` + nearest 필터를 사용해 인접 프레임 경계 bleed를 방지한다.

### Stage별 Hero 증강 풀 분리
- Hero profile은 `augment_pool_ids`를 가지며, 레벨업 후보는 해당 풀 안에서만 생성된다.
- 최대 중첩에 도달한 증강은 기존 규칙대로 후보에서 제외된다.
- Stage 1 견습 마도사는 현재 Hero 증강 8종 전체를 사용한다.
- Stage 2 기동 사냥꾼은 공격/연사/기동/저항/사거리/광역 중심 6종을 사용하며, 강인한 육체와 전투 회복은 후보에서 제외한다.
- 이 구조는 이후 Hero마다 고유 증강을 추가하거나 일부 공통 증강만 공유할 때도 profile 데이터만 바꾸는 방향으로 확장한다.

### Stage 1 통합 스프라이트시트 유지
- Stage 1 견습 마도사는 idle / move / attack / hit를 모두 하나의 384×256 통합 스프라이트시트에서 재생한다.
- 공통 셀은 64×64, 6열×4행이며 attack은 row 2의 6프레임 / 18 FPS / non-loop.
- 사용자가 직접 수정한 attack_01 / attack_05를 원본 셀 규격 그대로 통합 시트에 반영했다.
- 공격만 별도 크기의 시트로 분리하지 않아 상태 전환 시 캐릭터 체급/크기가 달라 보이지 않게 한다.
- 공격 아트 수정도 가능한 한 기존 64×64 셀과 anchor(x=32, foot_y=56)를 유지한 채 통합 시트에 반영한다.

### Stage별 Hero AI 성향 데이터 분리 v1
- Hero의 기본 전투 스탯/증강 풀과 Stage가 선택하는 AI 성향 데이터를 분리.
- `src/data/hero_ai_profiles.gd`에서 관측 주기, 기존 빌드 관성, 새 갈래 전환 저항, 증강별 성향 보정을 관리.
- `stage_catalog.gd`의 `hero_ai_profile_id`가 Stage별 AI 성향을 선택하며 Battle이 Hero 생성 전에 해당 데이터를 주입.
- 같은 Hero를 다른 Stage에서 재사용하더라도 Hero 스탯 파일을 복제하지 않고 AI 성격만 교체할 수 있는 구조.
- Stage 1은 기존 신중한 관측/강한 빌드 관성을 유지하고, Stage 2는 더 빠른 관측과 연사·기동 중심의 소폭 성향 보정을 사용.
- Build AI는 특정 Stage ID를 분기하지 않고 `augment_biases` Dictionary를 공통 계산에 더해 데이터만으로 성향을 확장.

### 메인 로비 / 5탭 메타 구조 v1
- 앱 기본 시작점을 전투 씬에서 `res://src/lobby/Lobby.tscn`으로 변경.
- 모바일 하단 네비게이션은 **상점 / 팀 편성 / 메인 / 연구 / 기타** 5탭을 기본 구조로 사용.
- 메인 탭은 Stage 선택 화면이며, Stage 카드 중앙에 해당 Stage 대표 Hero 초상화를 크게 배치하고 던전/침입자 기록 느낌의 프레임을 사용.
- Stage 데이터에 `portrait_path`와 `lobby_description`을 추가해 로비가 Stage별 표시 정보를 데이터에서 읽음.
- Stage 1 초상화 기본 경로는 `res://assets/art/heroes/stage1_mage/stage1_hero_portrait.png`.
- 초상화 파일이 아직 없거나 import되지 않은 환경에서도 Hero 이름 placeholder가 표시되도록 raw PNG → imported Texture 순서의 안전 로더 사용.
- Stage 선택 후 `던전 입장`을 누르면 기존 전투 씬 `src/main/Main.tscn`으로 이동하며 현재 Stage 진행 데이터는 그대로 재사용.
- 연구 탭은 기존 ResearchCatalog / StageProgress 구매 로직을 그대로 사용하고, 상점/팀 편성/기타는 우선 탭 자리만 확보.
- 전투 종료 결과 화면의 기존 스테이지 선택 버튼은 로비 복귀 버튼으로 전환.

### 전투 메뉴 / 로비 역할 분리 v1
- 메인 로비 도입 후에도 전투 HUD에 남아 있던 구형 Stage 선택 / 연구 팝업을 제거.
- 전투 상단의 `스테이지` 버튼은 `메뉴` 버튼으로 변경하고, 전투 중에는 **계속하기 / 현재 Stage 다시 도전 / 로비로 나가기**만 제공.
- 메뉴가 열린 동안 Battle의 external pause를 사용해 Hero / 몬스터 / 투사체 / EXP 구슬과 Run timer를 함께 정지.
- Stage 선택과 영구 연구 구매는 로비 전용 역할로 고정해 전투 중 메타 진행 UI와의 중복을 제거.
- Android 뒤로가기(`ui_cancel`)로 전투 메뉴 열기/닫기를 지원.

### 팀 편성 / 몬스터 덱 v1
- 로비의 팀 편성 탭에서 전투에 가져갈 몬스터를 **최대 3종 / 최소 1종** 선택.
- 편성은 `user://team_loadout.cfg`에 즉시 저장하며 기본 편성은 현재 카탈로그 순서의 슬라임 / 거미 / 오크.
- `TeamLoadout` 시스템이 저장된 ID를 MonsterCatalog 기준으로 검증해 삭제/변경된 콘텐츠 ID를 자동 무시.
- MonsterCatalog에 명시적 `ORDER`를 추가해 로비/기본 편성 순서가 Dictionary 순회 순서에 의존하지 않게 함.
- 전투 시작 시 Battle이 저장된 편성을 읽고, 편성되지 않은 몬스터의 소환 요청을 런타임에서도 거부.
- 전투 하단 3개 소환 버튼은 고정 Slime/Spider/Orc가 아니라 편성 슬롯 1~3의 몬스터 이름/실제 비용을 동적으로 표시.
- 편성 수가 3종보다 적으면 남는 전투 소환 버튼은 숨김.
- 이후 몬스터가 4종 이상으로 늘어나도 동일 저장/선택/전투 연결 구조를 재사용.

### 팀 편성 v1 회귀 롤백
- Android 실기기에서 팀 편성 v1 적용 직후 전투 HUD가 정적 상태로 멈추는 회귀 확인:
  - 남은 시간 미진행
  - 몬스터 소환 버튼 무반응
  - 전투 메뉴 버튼 무반응
- 전투/메뉴가 정상 동작하던 커밋 `4844cd5191bd80615a97560abcb7febe47b5a32e` 기준으로 관련 런타임 파일을 복구.
- 로비/Stage 선택/Stage 1 초상화/전투 일시정지 메뉴는 유지하고, 팀 편성 기능만 임시 제거.
- 팀 편성은 Android Godot에서 실제 오류 로그를 확인한 뒤 작은 단위로 다시 도입.

### 팀 편성 재구현 Step 1 — UI-only
- Android 전투 회귀 재발 방지를 위해 팀 편성을 단계적으로 다시 구현.
- Step 1은 `lobby.gd` / `Lobby.tscn`만 수정하며 Battle / Main 전투 코드 / 저장 시스템은 건드리지 않음.
- 팀 편성 탭에 3개 편성 슬롯, 보유 몬스터 3종 목록, 이름/역할/기본 비용 표시를 추가.
- MonsterCatalog를 읽어 비용순으로 표시하므로 로비 UI에 Slime/Spider/Orc ID를 직접 분기하지 않음.
- 몬스터 버튼을 탭하면 로비 세션 안에서만 선택 미리보기가 바뀌며 최소 1종 / 최대 3종 제한을 확인할 수 있음.
- 현재 선택은 저장되지 않고 전투 소환 슬롯에도 영향을 주지 않음.
- 실기기에서 기존 전투의 타이머/소환/메뉴가 유지되는지 확인 후 Step 2 저장 기능으로 진행.

### 팀 편성 재구현 Step 2 — 저장만 연결
- Step 1 UI-only 실기기 검증 후 편성 저장 기능만 추가.
- 신규 `src/systems/team_loadout_store.gd`가 `user://team_loadout.json`에 최대 3개의 몬스터 ID를 JSON 배열로 저장.
- 로비 진입 시 저장값을 읽고 MonsterCatalog의 현재 유효 ID와 대조해 삭제되거나 잘못된 ID는 자동 무시.
- 저장값이 없거나 전부 무효면 현재 보유 몬스터 중 비용이 낮은 순서 최대 3종을 기본 편성으로 사용.
- 최소 1종 / 최대 3종 제한은 유지하며 선택 변경 직후 자동 저장.
- **Battle / Main 전투 코드는 아직 저장값을 읽지 않음.** 따라서 전투 소환/타이머/메뉴 동작에는 영향이 없어야 함.
- Step 2 실기기 검증 후에만 Step 3에서 전투 소환 슬롯 연결 예정.

### 몬스터 컬렉션 / 조각 해금 기반 팀 편성 구조
- 향후 상점 뽑기에서 몬스터 조각을 획득하고 일정 수량에 도달하면 몬스터가 해금되는 구조를 전제로 팀 편성을 데이터 기반으로 재설계.
- `MonsterCatalog`가 표시 이름 / 역할 / 기본 비용뿐 아니라 `default_unlocked`, `shards_required`를 보유.
- 신규 `MonsterCollectionStore`가 `user://monster_collection.json`에 몬스터별 `unlocked` / `shards` 상태를 저장.
- 현재 Slime / Spider / Orc는 프로토타입 시작 몬스터이므로 기본 해금 상태.
- `MonsterCollectionStore.add_shards()`는 향후 상점 보상에서 호출할 수 있으며 요구 조각 이상이면 자동 해금.
- 팀 편성 UI는 Slime/Spider/Orc 고정 버튼을 제거하고 MonsterCatalog 전체를 순회해 카드 동적 생성.
- 해금 몬스터는 편성 토글 가능, 미해금 몬스터는 `조각 현재 / 필요량`을 표시한 잠금 카드로 노출.
- 편성 슬롯도 저장된 monster_id 목록을 기반으로 동적 생성하며 최대 3종 / 최소 1종 규칙 유지.
- 팀 탭에 들어올 때 Collection 상태를 다시 읽으므로 향후 상점에서 해금한 몬스터가 즉시 편성 목록에 반영 가능.
- 이 단계에서는 Battle/Main 전투 소환 슬롯과의 연결은 하지 않음.

### Supabase 백엔드 기반 v1
- Supabase 프로젝트: `xdmqpsyhtnyzzdhgvfep` / Seoul region.
- DB에 `profiles`, `player_progress`, `monster_collection`, `team_loadout` 테이블 생성.
- 모든 사용자 데이터 테이블에 RLS 적용. 로그인 사용자는 `auth.uid() = user_id`인 자기 행만 읽기/쓰기 가능.
- 신규 Auth user 생성 시 profile / player_progress 기본 행을 생성하는 trigger 사용.
- trigger용 SECURITY DEFINER 함수는 anon/authenticated RPC 실행 권한을 제거해 외부 직접 호출 차단.
- Godot 저장소에는 Supabase project URL과 **publishable key만** 포함. service_role/secret key는 절대 앱 또는 GitHub에 포함하지 않음.
- `src/network/supabase_client.gd`는 REST GET / upsert / delete 및 향후 access token 주입을 위한 기반만 제공.
- Google/Kakao/자체 로그인 UI 및 세션 발급은 후속 단계에서 연결.

### 팀 편성 로딩 안정화
- 팀/컬렉션 로컬 저장을 JSON Variant 변환 중심 방식에서 Godot 기본 `ConfigFile` 방식으로 단순화.
- `MonsterCollectionStore`는 Catalog를 순회해 기본 해금/조각 상태를 구성하며, 향후 상점 조각 지급 API는 그대로 `add_shards()`를 사용.
- `TeamLoadoutStore`는 `user://team_loadout.cfg`에 monster_id 목록을 문자열로 저장해 Android/PC에서 동일 경로로 복원.
- 팀 편성 초기화를 Lobby `_ready()`에서 제거하고 팀 탭 진입 시에만 수행. 편성 기능 문제가 있어도 메인 로비 전체 초기화를 막지 않음.
- Battle/Main 전투 코드는 이번 변경에서 수정하지 않음.

### 팀 편성 UI 정적 컨트롤 전환
- Android/PC에서 동적 Button 생성 기반 팀 편성 UI가 `편성 불러오는 중…` 상태에서 멈추는 문제 때문에 표시 경로를 단순화.
- 씬에 **편성 슬롯 Button 3개 + ItemList 1개**를 고정 배치. 고정되는 것은 슬롯 수(최대 3)뿐이며 몬스터 종류는 계속 MonsterCatalog 순회로 동적 표시.
- 팀 탭 진입 시 Catalog의 `default_unlocked`를 사용한 기본 편성을 먼저 즉시 렌더링.
- 저장된 MonsterCollection / TeamLoadout은 `call_deferred()`에서 다음 프레임에 읽어 덮어씀.
- 저장 데이터 로드에 문제가 생겨도 기본 편성 UI가 이미 표시되어 무한 로딩 빈 화면으로 남지 않도록 격리.
- 향후 상점 조각 해금 몬스터도 ItemList에 Catalog/Collection 상태를 기반으로 자동 반영.
- Battle/Main 전투 코드는 변경하지 않음.

### 팀 편성 UI 복구 기준점
- 팀 편성 무한/중단 로딩을 원인 분리하기 위해 UI 표시 경로를 MonsterCatalog 단독으로 축소.
- MonsterCollection / TeamLoadout 영구 저장 모듈은 파일로 유지하지만 현재 로비 팀 UI 초기화에서는 호출하지 않음.
- 팀 탭은 Catalog 전체 몬스터를 순회해 ItemList를 만들고 첫 최대 3종을 세션 기본 편성으로 사용.
- 슬롯/목록 탭을 통한 편성 추가/해제 동작을 실기기에서 먼저 검증한 뒤 저장 → 해금 상태 → 전투 연결 순으로 다시 확장.

### 팀 편성 Catalog-only 격리 v2
- 팀 탭 가시성 전환은 성공하지만 후속 UI 갱신이 실행되지 않는 증상을 확인.
- 팀 화면 초기화가 nav 버튼 스타일 처리보다 먼저 실행되도록 순서를 변경.
- 팀 UI에서 아직 필요하지 않은 MonsterCollection/TeamLoadout 저장 모듈 preload까지 제거해 Catalog 단독 경로로 최소화.
- 현재 편성은 세션 전용이며 MonsterCatalog의 등록 순서를 사용해 최대 3종을 자동 선택.
- 저장/해금 연동은 이 기준점이 PC/Android 양쪽에서 정상 동작한 뒤 재도입.

### 팀 편성 탭 전용 초기화 경로
- TeamButton은 generic tab handler를 사용하지 않고 전용 handler에서 TeamTab 표시 → MonsterCatalog 컬렉션 생성 → nav style 갱신 순으로 실행.
- 컬렉션 목록 생성은 편성 슬롯 표시와 분리해 ItemList에 먼저 Catalog 전체 항목을 추가.
- 편성 추가/해제 후에는 기존 ItemList의 text만 갱신해 목록 자체가 사라질 가능성을 줄임.
- 현재는 실기기 UI 검증 단계이므로 저장/해금 상태 연동은 제외.

### 팀 UI의 MonsterCatalog 접근 규칙 (임시 안정화)
- PC/Android에서 team handler는 실행되지만 MonsterCatalog static helper 호출 직후 UI 갱신이 멈추는 증상을 격리하기 위해 팀 UI는 Catalog의 상수 데이터(`ORDER`, `MONSTERS`, `ROLE_LABELS`)를 직접 읽음.
- 데이터 원천은 여전히 MonsterCatalog 하나이며 몬스터 ID/이름/역할/비용을 lobby에 복제하지 않음.
- 실기기 정상화 후 helper 호출 중단 원인을 별도 정리할 수 있으나 현재는 팀 편성 테스트 가능 상태 확보를 우선.

### 팀 편성 저장 재연결 — 안정화 기준
- Catalog-only 팀 편성 UI가 PC 실기 테스트에서 컬렉션 3종 표시와 추가/해제가 정상 동작함을 확인.
- 정상 UI를 먼저 렌더링한 뒤 `call_deferred()`로 `TeamLoadoutStore` 저장값만 복원해 저장 문제와 UI 표시 문제를 분리.
- 편성 추가/해제 성공 즉시 `user://team_loadout.cfg`에 monster_id 목록 저장.
- TeamLoadoutStore의 public return/internal result 배열은 untyped Array로 완화해 Android Variant 변환 부담을 줄임.
- MonsterCollection/조각 해금 상태와 Battle 소환 슬롯 연결은 아직 재연결하지 않음.
- 향후 팀 편성 UI는 **바둑판형 몬스터 카드 + 몬스터 도트 이미지** 구조로 개편 예정. 현재 ItemList는 기능 검증용.

### MonsterCollection 해금/조각 상태 재연결
- 팀 편성 UI/저장 기능이 PC에서 앱 재실행 후에도 정상 유지되는 것을 확인한 뒤 Collection 상태만 재연결.
- 팀 탭은 기존 Catalog 기반 기본 UI를 먼저 표시하고, deferred 단계에서 `monster_collection.cfg`와 `team_loadout.cfg`를 순서대로 복원.
- MonsterCollectionStore는 문제가 있었던 MonsterCatalog static helper를 호출하지 않고 `ORDER / MONSTERS` 상수 데이터만 직접 순회.
- 기본 해금 몬스터만 `team_available_ids`에 포함하며, 저장된 편성 중 잠긴/삭제된 monster_id는 자동 제외.
- 미해금 몬스터는 ItemList에 `[잠김] 이름 · 조각 현재 / 필요량`으로 표시하고 선택 불가.
- 현재 Slime / Spider / Orc는 Catalog의 `default_unlocked = true`이므로 모두 기본 해금.
- 향후 상점은 `MonsterCollectionStore.add_shards(monster_id, amount)`만 호출하면 조각 누적 및 요구량 도달 시 자동 해금 가능.
- Battle/Main 전투 편성 연결은 아직 재도입하지 않음.

### 저장 팀 편성 → 전투 소환 슬롯 표시 연결
- 로비에서 저장한 최대 3종의 팀 편성을 전투 진입 시 `TeamLoadoutStore`에서 읽는다.
- 전투 하단 소환 버튼은 현재 편성된 몬스터만 표시한다. 미편성 몬스터 버튼은 숨긴다.
- 편성 데이터는 `MonsterCollectionStore`의 현재 해금 목록으로 다시 정규화하여 잠긴/삭제된 몬스터가 전투 슬롯에 남지 않게 한다.
- 자동/수동 배치 모두 `_on_summon_pressed()`에서 편성 여부를 한 번 더 검증한다.
- 이번 단계에서는 Battle의 소환/시간/메뉴 처리 코드를 변경하지 않고 Main UI에서만 편성 표시/입력 제한을 연결한다.
- 향후 몬스터 수가 늘면 고정 3버튼 UI를 데이터 기반 동적 카드 UI로 교체한다.

### 팀 편성 → 전투 소환 UI 연결 Step 1
- 앱 재실행 후에도 로비 편성 저장/복원이 정상인 것을 확인한 뒤 전투 하단 소환 UI에 저장 편성을 연결.
- 기존 씬의 SlimeButton / SpiderButton / OrcButton 노드는 **콘텐츠 고정 버튼이 아니라 슬롯 1/2/3 UI 컨테이너**로만 재사용.
- 전투 진입 시 `team_loadout.cfg`를 직접 읽고, 슬롯 순서대로 버튼 텍스트/비용/소환 monster_id를 매핑.
- 편성이 1~2종이면 남는 소환 버튼은 숨김.
- 버튼 pressed signal도 기존 slime/spider/orc 고정 bind를 제거하고 slot index → saved monster_id 방식으로 변경.
- 이번 Step 1에서는 `Battle.gd` 자체의 소환 허용 규칙은 건드리지 않음. UI를 통한 일반 소환만 편성 슬롯을 사용.
- 다음 Step에서 실기기 회귀가 없는 것을 확인한 뒤 Battle 런타임 guard를 추가.

### 팀 편성 → Battle 런타임 제한 Step 2
- 전투 소환 UI가 저장된 팀 편성을 정상 반영하는 실기기 검증 후 Battle 내부 안전장치를 추가.
- `main.gd`가 이미 정규화한 `battle_loadout_ids`를 `Battle.set_allowed_monster_ids()`로 전달.
- Battle은 저장 파일 / TeamLoadoutStore를 직접 읽지 않아 전투 초기화와 영구 저장 책임을 분리.
- `_can_attempt_summon()`에서 편성 제한을 검사하므로 자동 소환 `try_summon()`과 수동 배치 `try_summon_at_position()` 모두 동일하게 적용.
- 비편성 monster_id 직접 소환 요청은 `현재 팀에 편성되지 않은 몬스터입니다.`로 거부.
- 전달된 편성이 비어 있으면 제한을 활성화하지 않는 fail-open 방식으로, 저장/전달 오류가 전투 전체를 막는 회귀를 방지.
- 이 단계로 **로비 편성 → 로컬 저장 → 전투 UI → Battle 런타임 소환 제한** 흐름이 완성됨.

### 팀 편성 바둑판 카드 UI v1
- 기능 검증용 세로 ItemList 표시를 **3열 카드형 ItemList**로 전환.
- 동적 Button 생성은 다시 도입하지 않고, 실기기에서 안정화된 ItemList 입력/선택 구조를 그대로 사용.
- 카드에는 이름 / 역할 / 기본 비용 / 편성 중 여부 또는 잠금 조각 진행도를 3줄로 표시.
- 편성 중 / 일반 / 잠금 상태별 배경/글자색을 구분하고 잠금 카드는 입력 비활성화.
- ItemList를 icon-top 모드와 고정 icon size로 설정해 실제 도트 이미지가 들어갈 자리를 미리 확보.
- MonsterCatalog에 선택적 `card_icon_path`가 있으면 로비의 기존 안전 Texture 로더로 자동 표시하도록 훅을 추가.
- 현재 저장소에는 monster art 폴더가 없으므로 v1은 텍스트 카드로 동작하고, 사용자가 몬스터 스프라이트를 커밋한 뒤 실제 파일을 확인해 카드 프레임을 연결.
- 편성 저장 / MonsterCollection / 전투 소환 슬롯 / Battle 런타임 제한 로직은 변경하지 않음.

### 팀 편성 카드 실제 몬스터 도트 연결
- 사용자가 커밋한 `assets/art/monsters/{slime,spider,orc}/frames/` 프레임 에셋을 팀 편성 카드 대표 이미지로 연결.
- 각 몬스터의 `idle_01.png`를 카드 대표 프레임으로 사용.
- MonsterCatalog의 각 몬스터 데이터에 `card_icon_path`를 추가:
  - Slime → `res://assets/art/monsters/slime/frames/idle_01.png`
  - Spider → `res://assets/art/monsters/spider/frames/idle_01.png`
  - Orc → `res://assets/art/monsters/orc/frames/idle_01.png`
- Lobby 카드 UI는 기존 `_team_monster_card_icon()` 훅으로 경로를 읽으므로 별도 monster_id 하드코딩 없이 표시.
- ItemList에 nearest texture filter를 적용해 확대 시 픽셀아트가 흐려지지 않도록 설정.
- 각 manifest의 animation frame 목록은 추후 실제 전투 idle/move/attack/hit/death 애니메이션 연결 때 사용 예정.
- 편성 저장 / 해금 / 전투 소환 / Battle guard 로직은 변경하지 않음.

### 몬스터 실제 전투 도트 애니메이션 v1
- 사용자가 제공한 개별 PNG 프레임을 전투 Slime / Spider / Orc에 연결.
- 공통 `MonsterVisual` AnimatedSprite2D 컴포넌트가 `frames/<animation>_NN.png` 규칙으로 프레임을 읽어 SpriteFrames를 런타임 구성.
- 프레임 리소스는 static cache로 공유해 몬스터 인스턴스마다 PNG 전체를 다시 구성하지 않도록 함.
- ResourceLoader 우선 + raw PNG Image fallback을 사용해 Android Editor import/cache 지연에도 비주얼 실패가 전투 스크립트 전체를 막지 않도록 함.
- 상태 연결:
  - 이동 중 → move loop
  - 정지/공격 대기 → idle loop
  - 공격 발생 → attack one-shot
  - 피격 → hit one-shot
  - 사망 → death one-shot 후 노드 제거
- Spider 에셋에는 hit 프레임이 없으므로 짧은 색상 플래시로 피격 피드백을 대체.
- 사망 신호는 기존처럼 HP 0 시 즉시 emit하여 EXP/사망 카운트/전투 판정 타이밍을 유지하고, 시각 노드 queue_free만 death 애니메이션 종료까지 지연.
- 원본 프레임 크기 차이는 target height 기준 자동 uniform scale로 보정:
  - Slime 80px
  - Spider 88px
  - Orc 104px
- 이미지 로딩 실패 시 기존 코드 드로잉 플레이스홀더를 fallback으로 유지.
- Battle 소환/편성/타이머/메뉴 로직은 변경하지 않음.

### 몬스터 전투 도트 애니메이션 회귀 수정
- 실제 도트 연결 직후 몬스터가 정지하고 Hero가 몬스터를 인식하지 못하는 회귀가 발생.
- 몬스터 AI 스크립트가 `MonsterVisual` custom class type에 직접 의존하지 않도록 수정.
- Slime / Spider / Orc의 `visual` 참조를 untyped child node로 두고, 모든 비주얼 상태 호출을 `has_method() + callv()` 경유로 변경.
- death signal도 custom signal 존재 여부를 확인한 뒤 동적으로 연결.
- Visual script/class 등록 또는 이미지 로딩이 실패해도 부모 CharacterBody2D의 `_ready()`, `add_to_group("monsters")`, `_physics_process()`, 이동/공격/피격 로직은 독립적으로 유지.
- 전투 핵심 AI가 비주얼 계층보다 우선하는 fail-soft 구조를 기준으로 유지.

### 상점 → 몬스터 조각 → 자동 해금 v1
- 로비 상점 탭의 플레이스홀더를 실제 몬스터 조각 상점으로 교체.
- v1에서는 기존 연구 포인트를 임시 상점 재화로 사용.
- 상품 데이터는 MonsterCatalog의 `shop_shard_amount / shop_shard_cost`에서 읽어 동적 생성:
  - Slime: 조각 +10 / 연구 포인트 15
  - Spider: 조각 +10 / 연구 포인트 20
  - Orc: 조각 +10 / 연구 포인트 25
- 구매 시 `StageProgress.try_spend_research_points()`로 포인트를 차감하고 `MonsterCollectionStore.add_shards()`로 조각 저장.
- 조각이 `shards_required`에 도달하면 기존 MonsterCollection 규칙으로 자동 해금.
- 해금된 몬스터는 다음 팀 편성 탭 진입 시 Collection 상태를 다시 읽어 즉시 선택 가능.
- 현재 Slime / Spider / Orc는 기존 안정화 상태를 보존하기 위해 `default_unlocked = true`를 유지. 향후 기본 잠금 몬스터를 추가하면 같은 상점 흐름으로 실제 잠금→해금 전환이 작동.
- 기본 해금 몬스터도 조각은 누적 가능하며, 현재는 해금 이후 조각 소비처(강화)는 후속 시스템 범위.
- Battle/팀 저장/몬스터 전투 애니메이션 로직은 변경하지 않음.

### 몬스터 조각 상자 가챠 v2
- 상점의 몬스터별 직접 조각 구매 방식을 제거하고 무작위 상자 가챠 방식으로 전환.
- 테스트 버전 보유 골드는 **99,999 고정 표시**이며 구매 시 실제 골드를 차감하지 않음.
- 구매 버튼:
  - 1회: 100골드
  - 10+1회: 1,000골드, 실제 추첨 11회
- 가챠 확률/조각 범위는 `shop_catalog.gd` 데이터로 분리:
  - 일반 70% / 10~30조각
  - 희귀 25% / 3~8조각
  - 최고등급 5% / 1조각
- 현재 몬스터 등급:
  - Slime = common
  - Spider = rare
  - Orc = legendary
- 추첨은 먼저 등급을 확률 가중치로 선택한 뒤 해당 등급 몬스터 중 1종을 균등 선택.
- 10+1은 보너스 확정 등급 없이 11회 독립 추첨. 보장 시스템은 후속 밸런스 단계에서 추가 가능.
- 당첨 조각은 `MonsterCollectionStore.add_shards()`로 즉시 누적하고 기존 자동 해금 규칙을 그대로 사용.
- 결과 UI는 몬스터별 획득 조각 총합과 새 해금 몬스터를 표시.
- 연구 포인트는 다시 연구 전용 자원으로 유지하고 상점에서는 사용하지 않음.

### 4번째 잠금 몬스터 — 폭탄쥐
- 상점 가챠 → 조각 → 해금 → 팀 편성 → 전투 전체 흐름 검증용 첫 기본 잠금 몬스터로 `bomb_rat` 추가.
- MonsterCatalog ORDER가 Slime / Spider / Orc / Bomb Rat 4종으로 확장.
- 폭탄쥐 데이터:
  - 이름: 폭탄쥐
  - 역할: burst / 표시명 폭발
  - 소환 비용 12
  - 기본 잠금 `default_unlocked = false`
  - 해금 요구 조각 30
  - 가챠 등급 rare
  - HP 36 / 이동속도 145 / 기본 공격 6 / EXP 32
  - 사망 폭발 반경 150 / 피해 28
- 폭탄쥐는 죽을 때 Hero가 폭발 반경 안에 있으면 추가 피해를 주는 최소 고유 기믹을 가짐.
- 현재 전용 아트가 없으므로 코드 드로잉 플레이스홀더를 사용하며 `card_icon_path`는 비워둠.
- 기존 가챠는 rarity → 해당 rarity 몬스터 균등 선택이므로 rare 풀에 Spider와 Bomb Rat가 함께 포함됨.
- MonsterCollectionStore / 팀 편성 / Main 전투 슬롯 / Battle 소환은 Catalog 순회 기반이므로 별도 콘텐츠 ID 분기 없이 4번째 몬스터를 수용.
- 실기기에서 조각 누적, 30조각 자동 해금, 편성 추가, 전투 소환까지 확인한 뒤 4종 덱 확장 검증 완료로 판단.

### Hero 필살기 게이지 v1
- 모든 Hero가 공통 `ultimate_config`와 `ultimate_charge`를 갖는 구조 추가.
- 각 Hero의 필살기 설정은 `hero_profiles.gd`에 데이터로 저장하며 Hero 본체는 공통 실행 타입을 처리.
- 충전원 v1:
  - 시간 경과
  - 기본 공격 발사
  - 실제 피해를 받았을 때 피해량 비례
- 게이지가 `charge_max`에 도달하면 AI Hero가 자동으로 필살기를 사용하고 게이지를 0으로 초기화.
- Hero 월드 HP바 바로 위에 노란색 필살기 게이지를 표시.
- 발동 순간 Hero 주변에 짧은 노란 원형 플래시를 표시해 필살기 사용을 인지할 수 있게 함.
- 공통 필살기 타입 v1: `area_burst` — 설정 반경 내 살아있는 몬스터들에게 기존 `take_damage()` 경로로 피해.
- Stage 1 견습 마도사:
  - `arcane_nova / 마력 폭발`
  - 최대 게이지 100
  - 초당 +2.5 / 공격 발사 +4 / 받은 피해 1당 +0.25
  - 반경 520 / 피해 90
- Stage 2 기동 사냥꾼:
  - `hunter_volley / 사냥꾼의 일제사격`
  - 최대 게이지 100
  - 초당 +3 / 공격 발사 +3 / 받은 피해 1당 +0.20
  - 반경 460 / 피해 72
- Battle의 pause는 Hero physics를 정지시키므로 필살기 시간 충전도 함께 정지.
- 필살기 피해도 기존 Monster `take_damage → died` 흐름을 사용해 EXP/사망 카운트/보상 처리를 우회하지 않음.
- 신규 Hero가 동일한 공통 필살기 타입을 쓰는 경우 HeroProfiles 데이터 추가만으로 연결 가능.

### Run 종료 연구 포인트 v1
- 패배해도 연구가 누적되어 영구 연구를 구매하고 재도전할 수 있도록, 매 정상 Run 종료 시 연구 포인트를 지급한다.
- 지급식:
  - 기본 참여 +5
  - Hero 최대 HP의 10%를 깎을 때마다 +2, 최대 +20
  - 전투 경과 60초마다 +1, 최대 +10
  - Hero 증강 선택 관찰 1회당 +1, 최대 +5
- 반복 Run 보상 최대치는 40.
- 시간 초과 패배와 Hero 처치 승리 모두 반복 보상을 지급한다.
- 최초 클리어 보상은 Stage 데이터의 기존 100/150 포인트를 별도로 유지해 반복 보상과 중첩된다.
- 재시작/로비 이탈처럼 정상 Run 종료가 아닌 경우 반복 보상을 지급하지 않는다.

### 연구 포인트 경제 v2
- 정상 Run 종료 보상은 패배 누적 성장을 확실히 체감할 수 있도록 약 110~160 연구 포인트 구간을 목표로 한다.
- 반복 Run 연구 보상:
  - 기본 +80
  - Hero 최대 HP 10% 감소마다 +6, 최대 +60
  - 60초 경과마다 +5, 최대 +30
  - Hero 증강 관찰 1회당 +4, 최대 +20
  - 총 반복 보상 상한 160
- Stage 1 최초 클리어 보상은 500, Stage 2는 750으로 상향한다.
- 연구 비용도 새 경제에 맞춰 1레벨 약 150~220, 후속 레벨 250~500 범위로 조정한다.
- 영구 연구 효과를 강화:
  - 마력 저장고: 최대 지휘력 +20/레벨
  - 마력 순환: 지휘력 회복 +0.5/초/레벨
  - 슬라임 배양 최적화: 슬라임 비용 -8%/레벨
  - 전술 기록 노트: 증강 새로고침 +1/레벨 유지
  - 고속 실험법: 마왕 EXP 획득 +10%/레벨

### 영구 연구 기본 스탯 구조 v1
- 패배 Run을 반복해 연구 포인트를 모으고 군단 전체를 강화해 스테이지를 돌파하는 내실 루프를 핵심 성장 구조로 사용한다.
- 기본 연구 축:
  - 군단 화력: 모든 몬스터 공격력 +10%/레벨
  - 군단 생체 강화: 모든 몬스터 최대 HP +12%/레벨
  - 군단 기동: 모든 몬스터 이동속도 +8%/레벨
  - 군단 공격 훈련: 공격 주기 -7%/레벨, 폭탄쥐 자폭 준비시간에도 적용
  - 소환 효율화: 모든 몬스터 소환 비용 -5%/레벨
  - 마력 순환: 지휘력 회복 +0.5/초/레벨
  - 마력 저장고: 최대 지휘력 +20/레벨
  - 고속 실험법: 마왕 EXP 획득 +10%/레벨
- 전술 기록 노트(+증강 새로고침)는 보조 연구로 유지한다.
- 특정 몬스터 전용 수치 연구보다 공통 군단 연구를 먼저 제공하고, 몬스터별 고유 연구는 추후 특성 연구로 확장한다.

### 연구 비용 곡선 v2
- 영구 연구는 초반 첫 구매 진입 장벽을 낮추고, 후반 레벨일수록 비용 증가폭을 크게 잡는다.
- 5레벨 연구는 대체로 Lv.1 35~50 → Lv.2 80~130 → Lv.3 160~260 → Lv.4 290~420 → Lv.5 480~650 구간을 사용한다.
- 목표는 패배 Run 1회 보상으로 초반 연구 2~3개를 찍을 수 있게 하되, 후반 완성에는 여러 Run이 필요하도록 만드는 것이다.
- 연구 효과 수치는 이번 조정에서 변경하지 않는다.

### 장기 영구 연구 레벨/비용 구조 v3
- 기본 전투 스탯 연구(공격/체력/이동속도/공격속도)는 최대 70레벨의 장기 누적 성장으로 운용한다.
- 기본 스탯 효과:
  - 공격력 +1%/Lv, 최대 +70%
  - 최대 HP +1%/Lv, 최대 +70%
  - 이동속도 +0.35%/Lv, 최대 +24.5%
  - 공격속도 +0.5%/Lv, 최대 +35% 공격속도
- 공격속도는 쿨다운을 선형 차감하지 않고 `1 / (1 + 보너스)` 방식으로 계산해 고레벨에서도 안전하게 누적한다.
- 기본 스탯 연구는 Lv.1 비용 10~15로 매우 낮게 시작하고 약 6.5~6.8%의 지수 성장률로 레벨이 오를수록 비용 증가폭이 커진다.
- 경제/자원 연구:
  - 소환 효율화: 최대 20레벨, 비용 -1%/Lv(최대 -20%)
  - 마력 순환: 최대 30레벨, 지휘력 회복 +0.15/초/Lv
  - 마력 저장고: 최대 30레벨, 최대 지휘력 +5/Lv
  - 고속 실험법: 최대 20레벨, 마왕 EXP +3%/Lv
- 경제/자원 연구는 기본 스탯보다 높은 70~90 포인트에서 시작하며 약 11.5~14%의 비용 성장률을 사용한다.
- 연구 비용은 고정 배열 대신 `base_cost * cost_growth^current_level` 공식으로 계산하고 5포인트 단위로 반올림한다.

### 기본 스탯 연구 비용 곡선 v4
- Stage 1 최초 클리어 500 + Run 보상으로 기본 스탯 하나가 약 20레벨까지 즉시 올라가던 속도를 완화한다.
- 기본 스탯의 첫 비용 10~15는 유지하되 비용 성장률을 크게 올린다.
  - 공격력: 15 시작 / 레벨당 비용 성장률 15%
  - 체력: 15 시작 / 레벨당 비용 성장률 15%
  - 이동속도: 10 시작 / 레벨당 비용 성장률 16%
  - 공격속도: 15 시작 / 레벨당 비용 성장률 15%
- 초반 몇 레벨은 쉽게 구매할 수 있지만 10레벨 이후부터 요구 연구 포인트가 빠르게 증가하도록 한다.
- 전술 기록 노트는 일반 비용 공식 대신 고정 비용 1000 / 3000 / 5000 연구 포인트를 사용한다.

### Stage 1 용사 필살기 확장 v2
- 기존 Stage 1 견습 마도사의 자동 area_burst 필살기를 직선 관통형 필살기 `마력 관통포`로 교체한다.
- 기존 노란 필살기 게이지 충전 규칙은 유지한다.
- 발동 시 가장 가까운 몬스터 방향으로 대형 투사체를 발사하며, 경로상의 여러 몬스터를 각각 1회 관통 타격한다.
- 초기 밸런스: 피해 110 / 사거리 900 / 속도 950 / 충돌 폭 약 116×56.
- 사용자 제공 `stage1_mage/effect_01` 6프레임 픽셀 이펙트를 투사체 비주얼로 사용한다.
- Stage 1 필살기 2(실드), 3(채널링 광역 지속 피해)는 후속 단계에서 각각 분리 구현한다.

### Stage 1 용사 필살기 2 — 마력 장벽
- 관통포와 별도 쿨다운으로 작동하는 방어 스킬.
- 초기 쿨다운 8초, 이후 쿨다운 18초.
- HP 75% 이하 또는 반경 300px 안에 몬스터 3마리 이상일 때 자동 발동.
- 보호막은 Hero 최대 HP의 30%를 별도 Shield HP로 생성하고 8초 동안 유지.
- 피해는 Shield HP가 먼저 흡수하며 남은 피해만 Hero HP에 적용.
- 보호막 활성 중 Hero HP바 아래에 파란 Shield 게이지를 표시.
- 사용자 제공 `stage1_mage/effect_02` 6프레임을 보호막/마법진 루프 이펙트로 사용.

### Stage 1 용사 스킬 3 — 비전 집중
- 관통포/마력 장벽과 별도 쿨다운으로 작동하는 제자리 채널링 광역 지속 피해 스킬.
- 초기 쿨다운 12초, 이후 쿨다운 16초.
- 반경 210px 안에 몬스터가 4마리 이상일 때 자동 발동.
- 2.5초 동안 이동을 멈추고 채널링하며 0.25초마다 주변 적에게 14 피해.
- 채널링 중에는 일반 이동/기본 공격을 하지 않는다.
- 사용자 제공 `stage1_mage/effect_03` 6프레임을 채널링 동안 루프 이펙트로 사용.

### Stage 1 노란 필살기 게이지 운용 규칙
- 마력 장벽(2번)은 노란 필살기 게이지와 무관한 독립 방어 스킬로 유지한다.
- 마력 관통포(1번)와 비전 집중(3번)은 같은 노란 게이지 100%를 소비하는 필살기 후보이다.
- 두 필살기는 각각 독립 쿨다운을 가진다.
  - 마력 관통포: 14초
  - 비전 집중: 16초
- 게이지가 100%일 때 주변 210px 내 몬스터가 4마리 이상이고 비전 집중 쿨다운이 준비되면 3번을 우선 사용한다.
- 그렇지 않고 관통포 쿨다운이 준비되면 1번을 사용한다.
- 둘 다 쿨다운 중이면 노란 게이지를 100% 상태로 유지하며 사용 가능해질 때까지 기다린다.
- 1번 또는 3번 중 하나를 사용한 순간 노란 게이지를 0으로 소모한다.

### Stage 1 필살기 선택 AI / 쿨다운 밸런스 v2
- 노란 게이지 100%에서 1번/3번을 단순 우선순위로 고르지 않고 전황에 따라 역할을 분리한다.
- 마력 관통포(1번):
  - 쿨다운 14초 → 12초.
  - 범용 공격 필살기로 사용.
  - 가장 가까운 적만 조준하지 않고, 사거리 900 내 몬스터 후보 방향을 비교해 폭 약 144px의 직선 회랑에 가장 많은 몬스터가 들어오는 방향을 선택한다.
- 비전 집중(3번):
  - 쿨다운 16초 → 18초.
  - 반경 210px 안에 5마리 이상이면 즉시 사용 조건 충족.
  - 또는 반경 210px 안에 4마리 이상이면서 그중 2마리 이상이 135px 안까지 붙었을 때 사용.
  - 단순 다수 적보다 실제 포위/근접 압박 상황에서 우선 사용하도록 역할을 좁힌다.
- 3번 조건이 충족되지 않으면 1번이 준비된 경우 관통포를 사용한다.
- 둘 다 쿨다운이면 노란 게이지는 100% 유지한다.
- 마력 장벽(2번)은 이 선택 AI와 무관한 독립 방어 스킬로 유지한다.

### 마왕 필살기 공용 게이지 / 원형 포위 v1
- 마왕은 인게임에서 공용 필살기 게이지 1개를 사용하며, 게이지 100에서 3개 기술 중 1개를 직접 선택해 발동하는 구조를 사용한다.
- 게이지 충전 v1:
  - 시간 경과 초당 +1
  - 일반 몬스터 소환 시 실제 소환 비용 ×1.2
  - Hero 실제 HP 피해 1당 +0.25
- 필살기로 생성된 무료 몬스터는 지휘력과 마왕 EXP를 소비/획득하지 않아 자기충전 루프를 만들지 않는다.
- HUD의 기존 하단 패널 바로 위에 마왕 필살기 게이지 + 3개 버튼을 배치한다.
- 1번 `원형 포위` v1:
  - 현재 편성 몬스터를 사용.
  - Hero 기준 반경 약 700px의 원주에 12마리를 균등 배치.
  - 편성 슬롯 순서대로 몬스터 종류를 순환해 소환.
  - 맵 가장자리에서는 유효 전장 범위 안으로 좌표를 보정.
- 2번 `일직선 공세`, 3번 `사각 포위`는 버튼/데이터 구조만 먼저 만들고 실제 발동은 후속 단계에서 구현한다.
- 엘리트/미니보스/최종 보스 출현은 플레이어 필살기와 분리된 Stage Director 타임라인 시스템으로 후속 구현한다.

### 전투 HUD 정리 v1
- 기능 검증용 디버그 정보와 실제 플레이 정보를 분리한다.
- 전투 중 기본 화면에서는 DebugBalance 텍스트를 숨기고, 플레이 핵심 정보만 유지한다.
- 하단 정보 우선순위:
  1. 용사 빌드/현재 상태 한 줄 요약
  2. 마왕 Lv/EXP
  3. 지휘력/자동 배치
  4. 편성 몬스터 3개 소환 카드
- 마왕 필살기 영역은 하단 패널 바로 위의 별도 얇은 스트립으로 유지하며, 게이지 + 3버튼을 한눈에 보이도록 축소 정렬한다.
- 이번 정리는 레이아웃만 조정하며 전투/소환/필살기 로직은 변경하지 않는다.

### 전투 화면 / 마왕 필살기 HUD 분리
- 마왕 필살기 스트립은 전투 화면 위에 덮는 오버레이가 아니라 전투 영역과 하단 HUD 사이의 독립 고정 영역으로 사용한다.
- BattleViewportContainer 하단 경계를 필살기 스트립 상단까지 올려 몬스터/투사체가 UI 뒤에 가려지지 않도록 한다.
- 모바일 세로 화면에서 정보 가독성을 위해 전투 영역 일부를 줄이는 대신, 실제 전투 화면은 항상 온전히 보이도록 한다.

### 마왕 필살기 2 — 일직선 공세 v1
- 공용 마왕 필살기 게이지 100에서 2번 버튼을 선택하면 동/서/남/북 방향 선택 모드로 전환한다.
- 방향 선택 UI는 전투 화면을 덮는 팝업이 아니라 기존 필살기 스트립 내부에서 1/2/3 버튼을 잠시 대체한다.
- 선택 방향 기준 Hero에서 약 700px 떨어진 한쪽 면에 편성 몬스터 10마리를 약 720px 길이의 직선으로 균등 배치한다.
- 현재 편성 몬스터를 슬롯 순서대로 순환해 사용한다.
- 1번과 동일하게 지휘력/마왕 EXP를 소비·획득하지 않는다.
- 순간 부하 방지를 위해 0.04초 간격으로 2마리씩 분산 생성한다.
- 방향 선택 취소 시 게이지는 소비하지 않는다.

### 마왕 필살기 개별 쿨타임 UI v1
- 공용 필살기 게이지와 별개로 각 마왕 필살기에 독립 쿨타임을 둔다.
- 초기 임시 쿨타임:
  - 1번 원형 포위: 18초
  - 2번 일직선 공세: 16초
  - 3번 사각 포위: 20초(기능 구현 전 예약값)
- 기술 사용 직후 해당 기술 쿨타임만 시작하며 다른 필살기 쿨타임에는 영향을 주지 않는다.
- 공용 게이지가 100이어도 선택한 기술의 쿨타임이 남아 있으면 그 버튼만 비활성화한다.
- 필살기 버튼 하단에 회색 쿨타임 게이지를 표시하며 남은 시간에 따라 실시간으로 줄어든다.
- 버튼 텍스트에 `쿨타임 NN.N초`를 0.1초 단위로 갱신한다.

### 마왕 필살기 3 — 사각 포위 v1
- 공용 마왕 필살기 게이지 100에서 3번 버튼으로 직접 발동한다.
- Hero 기준 상/하/좌/우 약 700px 외곽을 기준으로 사각형 테두리를 만들고, 그 둘레에 편성 몬스터 16마리를 균등 배치한다.
- 원형 포위보다 방향성이 강한 네 면 봉쇄를 만들어 Hero의 카이팅/도주 공간을 압박하는 역할로 구분한다.
- 현재 편성 몬스터를 슬롯 순서대로 순환해 소환한다.
- 지휘력과 마왕 EXP를 소비·획득하지 않는다.
- 0.04초 간격으로 2마리씩 분산 생성한다.
- 개별 쿨타임은 20초이며 기존 회색 쿨타임 바와 `NN.N초` 실시간 표시를 그대로 사용한다.

### 1080×1920 유지형 HUD 가독성 스케일 업
- 프로젝트 논리 해상도 1080×1920은 유지한다.
- 해상도 변경 대신 전투 HUD 폰트와 주요 터치 UI를 약 20~30% 키워 실제 모바일 화면에서 가독성을 높인다.
- 상단 Hero/시간/EXP 정보, 마왕 필살기 버튼, 하단 상태/마왕 EXP/지휘력/소환 카드의 글자 크기를 우선 확대한다.
- 게이지 두께도 함께 소폭 확대하되 전투 영역 크기와 터치 좌표 변환 로직은 변경하지 않는다.
- 현재 레이아웃 구조와 필살기/소환 기능은 그대로 유지한다.

### Stage Director v1 — 시간 기반 엘리트/미니보스/보스
- 마왕 필살기와 별개로 Stage 자체가 시간에 따라 이벤트를 발생시키는 `StageDirector` 시스템을 사용한다.
- Stage별 이벤트는 `stage_catalog.gd`의 `event_timeline` 데이터로 관리하며 Battle 코드에 시간/몬스터를 하드코딩하지 않는다.
- Stage 1 프로토타입 타임라인:
  - 01:30 정예 거미
  - 03:00 정예 오크
  - 04:00 오크 대장(미니보스)
  - 05:00 침공 대장(보스, 6분 Run 종료 1분 전)
- 현재는 전용 엘리트/보스 아트가 없으므로 기존 Spider/Orc를 HP/공격력/속도/크기/EXP 배수로 강화해 프로토타입 검증한다.
- Stage Director 몬스터는 플레이어 지휘력이나 마왕 EXP를 소비하지 않는다.
- 향후 전용 elite/miniboss/boss monster_id가 추가되면 타임라인 데이터 교체만으로 연결할 수 있도록 한다.

### Stage Director v2 — 편성 기반 돌연변이 선택
- 01:30 / 03:00 / 04:00 이벤트는 자동 엘리트 소환 대신 전투를 일시정지하고 돌연변이 선택 모달을 연다.
- 선택지는 현재 일반 몬스터 팀 편성 3종을 그대로 사용한다.
- 01:30 / 03:00 선택 결과는 `돌연변이 <몬스터명>`, 04:00은 `대돌연변이 <몬스터명>`으로 표시한다.
- 선택된 몬스터는 해당 Stage 이벤트의 HP/공격력/속도/EXP/크기 배율을 적용받아 강화 개체 1마리로 등장한다.
- 돌연변이는 Hero 기준 약 460px 거리에서 생성해 선택 직후 화면에서 확인하기 쉽게 한다.
- 선택 모달이 열린 동안 Run 타이머와 전투 physics를 정지한다.
- 05:00 최종 보스 이벤트는 다음 단계의 `일반 3 + 보스 1` 팀 편성 시스템 연결 전까지 기존 Stage 고정 프로토타입을 유지한다.

### Stage Director v2.1 — 돌연변이 선택 직결
- 돌연변이 모달 버튼은 별도 resolver/성공 실패 검증/deferred 단계를 거치지 않는다.
- UI는 현재 편성 후보의 `monster_id`만 Battle에 전달한다.
- Battle은 자신이 보관 중인 `pending_mutation_event`에 이름/스폰 거리만 적용한 뒤 즉시 강화 몬스터 1마리를 생성한다.
- 생성 직후 돌연변이 선택 상태를 해제하고 전투 physics를 재개한다.
- 기본 전투의 시간/지휘력/일반 소환 루프는 변경하지 않는다.

### MutationDirector v1 — 돌연변이 상태 소유권 분리
- StageDirector는 시간에 따른 이벤트 발생만 담당한다.
- 새 `src/systems/mutation_director.gd`가 돌연변이 선택의 활성 상태, 현재 Stage 이벤트 데이터, 후보 monster_id 목록을 단독으로 소유한다.
- Main UI는 `mutation_choice_ready`로 받은 후보를 표시하고 선택한 monster_id만 Battle에 전달한다.
- Battle은 MutationDirector가 확정한 이벤트를 받아 실제 강화 몬스터 생성과 combat physics 재개만 담당한다.
- Battle 내부의 돌연변이 active/pending/candidate 중복 상태를 제거한다.
- 향후 돌연변이 등급/재선택/특수 규칙은 MutationDirector 또는 데이터 카탈로그 쪽에 확장하고 Main/Battle에 개별 상태를 추가하지 않는다.

### MutationCatalog v1 — 돌연변이 규칙 데이터 분리
- 새 `src/data/mutation_catalog.gd`가 돌연변이 등급별 수치/표시 규칙을 관리한다.
- 현재 프로필: `mutation_1`, `mutation_2`, `greater_mutation`.
- 프로필 데이터에는 HP/공격력/속도/EXP/크기 배율, Hero 기준 스폰 거리, 이름 접두사, 모달 제목/설명이 포함된다.
- Stage의 mutation 이벤트는 구체 배율을 직접 보관하지 않고 `mutation_profile_id`만 참조한다.
- MutationDirector가 Stage 이벤트와 MutationCatalog 프로필을 합쳐 Main/Battle에 전달한다.
- Battle/Main은 elite/miniboss별 표시/배율 하드코딩을 하지 않는다.
- 새 돌연변이 등급 추가 시 MutationCatalog 프로필 추가 + Stage의 profile_id 지정만으로 확장하는 것을 원칙으로 한다.

### Special Monster Spawn v1 — 특수 스폰 공통 경로
- 돌연변이와 Stage 고정 보스가 공통 `Battle.spawn_special_monster(monster_id, special_data)` API를 사용한다.
- 공통 특수 스폰 API는 monster_id 검증, Hero 기준 스폰 거리 계산, HP/공격력/속도/EXP/크기 배율 적용, 특수 이벤트 메타데이터 설정만 담당한다.
- Stage 이벤트 알림 문구/시그널은 `_emit_stage_event_announcement()`로 분리해 실제 생성 로직과 UI 알림 책임을 분리한다.
- 기존 `_spawn_stage_event_monster()`는 제거한다.
- 향후 편성 보스, 이벤트 보스, 돌연변이 외 특수 몬스터도 같은 API를 재사용하고 일반 소환/지휘력 경로와 분리한다.
- 일반 몬스터 소환의 `_spawn_monster()`, 지휘력 소비, Run 시간 루프는 변경하지 않는다.

### Elite Monster Visual Profiles v1
- 일반 몬스터 ID와 엘리트/돌연변이 ID를 분리하지 않는다. 동일 monster_id에 돌연변이 프로필과 비주얼 variant를 조합한다.
- MonsterCatalog의 각 몬스터 데이터가 선택적으로 elite_visual 프로필을 가진다.
- Slime/Spider/Orc는 공통 MonsterVisual.apply_visual_profile()로 런타임 비주얼만 교체한다.
- Bomb Rat은 자체 시트 애니메이션 구조를 유지하되 동일한 apply_visual_profile() 인터페이스를 제공한다.
- MutationCatalog는 돌연변이 배율/규칙, MonsterCatalog는 몬스터별 비주얼을 소유한다.
- Bomb Rat 폭발은 통합 effect sheet 대신 assets/art/monsters/bombrat/frames/frame_01~08.png 개별 프레임을 사용한다.

### Spider Ranged Controller v1
- Spider는 더 이상 근접 즉시타격 몬스터가 아니다.
- Hero와 약 300px 이내까지 접근한 뒤 멈춰 느린 원거리 투사체를 발사한다.
- 기본 투사체 속도 320 / 최대 이동거리 360 / 공격 주기 1.35초.
- 투사체 적중 시 기존 제어 정체성을 유지해 Hero에게 72% 이동속도 둔화를 1.5초 적용한다.
- 일반 Spider 투사체는 assets/art/monsters/spider/frames/effect/frame_01~08.png,
  엘리트 Spider 투사체는 assets/art/elitemonster/spider/frames/effect/frame_01~08.png를 사용한다.
- Spider는 복잡한 카이팅을 하지 않고 접근 → 사거리 진입 → 정지 → 발사의 단순 AI를 유지한다.


### 마왕 성장/증강 밸런스 v2
- 몬스터 소환 지휘력 비용과 마왕 EXP를 분리해 MonsterCatalog의 `summon_exp`를 성장 기준으로 사용한다.
- 마왕 레벨 요구 EXP 곡선은 `15 + (레벨 - 1) * 4`로 완화해 첫 특수증강(Lv.10)과 후속 Lv.15/Lv.20 특수증강이 실제 Run 안에서 더 자주 등장하도록 조정한다.
- 일반증강에 `침략 학습` 추가:
  - 최대 Lv.20
  - 레벨당 몬스터 소환 마왕 EXP 획득 +5%p
  - 최대 Lv.20에서 기본 대비 +100%
- 비용 감소는 지휘력 경제만 개선하고, 마왕 EXP 효율을 직접 낮추지 않는다.
- 마왕 증강 선택 카드는 모바일 세로 화면에서 긴 설명이 화면 밖으로 밀려나지 않도록 카드 폭을 균등 분배하고 코드 기반 줄바꿈을 적용한다.


### 로비 몬스터 상세보기 v1
- 팀 편성 화면에서 마지막으로 탭한 몬스터를 대상으로 `상세보기` 버튼을 제공한다.
- 기존 카드 탭 편성/해제 동작은 유지하고, 상세보기는 별도 버튼으로 열어 편성 UX를 깨지 않는다.
- 상세 화면은 좌/우 비교 구조:
  - 좌측: 일반 몬스터 기본 스탯, 기본 소환 비용, `summon_exp`, 역할, 획득 가능한 특수증강 3종.
  - 우측: 엘리트 비주얼과 1차 돌연변이(`mutation_1`) 기준 스탯/배율.
- 로비는 Run 보정 전 도감 성격으로 운용하므로 마왕 레벨, 영구 연구, Run 일반증강을 합산한 최종 스탯은 표시하지 않는다.
- 엘리트 수치는 실제 이벤트마다 변경될 수 있으므로 `기본 돌연변이 기준`으로 명시한다.
- 일반/엘리트 미리보기는 기존 PNG 리소스를 그대로 사용하며 별도의 엘리트 시스템과 특수증강 시스템을 합치지 않는다.


### UI 가독성 스케일 v1
- 렌더링/뷰포트 해상도 1080x1920은 유지한다.
- 해상도를 낮춘 것처럼 체감되도록 Lobby/Main UI의 명시적 폰트 크기를 약 25% 확대한다.
- 주요 Button/CheckButton의 최소 높이는 약 15% 확대해 터치성과 가독성을 함께 보정한다.
- 가로 폭은 가능한 기존 Container/anchor 레이아웃을 유지해 화면 밖 밀림을 피한다.
- 로비 몬스터 상세 팝업은 전체 UI 확대와 별개로 중앙 고정형으로 압축:
  - 약 960x1300
  - 일반/엘리트 초상 높이 180
  - 상하 여백/간격 축소
- 향후 폰트 교체 시 개별 Label마다 파일을 직접 지정하지 않고 Theme 기본 폰트로 통합 적용하는 방향을 우선한다.


### 로비 팀 카드 액션 분리 v2
- 팀 편성 컬렉션 UI는 기존 ItemList의 `카드 탭 = 편성/해제` 동작을 제거한다.
- 각 몬스터를 독립 카드로 렌더링하고 카드 하단에 두 개의 명시적 액션 버튼을 둔다.
  - `팀 편성` / `편성 해제`
  - `상세정보`
- 상세정보를 열어도 팀 편성 상태는 변경되지 않는다.
- 편성 상태 변경 후 카드를 즉시 다시 그려 버튼 문구와 편성 상태 표시가 동기화된다.
- 잠긴 몬스터는 팀 편성 버튼만 비활성화하고 상세정보는 확인할 수 있다.
- 편성 슬롯 최대 3종, 최소 1종 유지 규칙은 기존과 동일하다.


### 공용 게임 폰트 v1
- 전체 UI 기본 폰트는 `Galmuri11.ttf`를 사용한다.
- 기대 경로: `res://assets/fonts/Galmuri11.ttf`.
- `GameFontManager`를 Autoload로 등록해 Lobby/Main/CanvasLayer HUD 및 런타임 생성 Control까지 같은 폰트를 적용한다.
- 폰트 파일이 아직 없으면 경고만 남기고 기존 Godot 기본 폰트로 안전하게 fallback한다.
- 개별 Label/Button마다 폰트 파일을 직접 지정하지 않고 공용 매니저를 통해 일괄 적용한다.


### 인게임 몬스터 정보 패널 v1
- 전투 HUD 우측에 책갈피형 `도감` 버튼을 두고, 누르면 오른쪽에서 약 0.22초 슬라이드 인되는 몬스터 상세 패널을 연다.
- 패널을 열어도 FlowPauseManager에 pause reason을 추가하지 않는다. 전투/타이머/지휘력/마왕 런타임은 계속 진행된다.
- 현재 편성 3종을 탭으로 전환하며 현재 Run 기준 값을 표시한다:
  - 현재 생산비용, summon_exp
  - 마왕 레벨 및 레벨 보정이 반영된 HP/공격력/이동속도
  - 현재 공격간격, 폭탄쥐 자폭 피해/반경/준비시간
  - 해당 몬스터 일반증강 레벨
  - 해당 몬스터 획득 특수증강
- 패널이 열린 동안 마왕 레벨업 또는 증강 획득이 발생하면 표시 값을 즉시 갱신한다.
- Galmuri11 적용 후 18~21px 작은 설명 텍스트는 가독성을 위해 +2px 보정한다.
- 폭탄쥐 effect PNG의 복사된 .import 메타데이터는 UID 중복을 일으키므로 저장소에서 제거하고 Godot이 새 UID로 재생성하도록 한다.


### 몬스터 기본 스탯 데이터 원본 통합 v1
- 몬스터 기본 스탯은 상세 UI가 씬을 임시 instantiate해서 읽지 않고 MonsterCatalog의 `base_stats`를 원본으로 사용한다.
- 현재 등록 값:
  - 슬라임: HP 60 / 이동속도 115 / 공격력 9 / 사거리 72 / 공격간격 1.10
  - 거미: HP 45 / 이동속도 150 / 공격력 5 / 사거리 300 / 공격간격 1.35 / 둔화 0.72 / 1.5초
  - 오크: HP 140 / 이동속도 78 / 공격력 18 / 사거리 82 / 공격간격 1.45
  - 폭탄쥐: HP 36 / 이동속도 175 / 자폭거리 78 / 자폭준비 0.30 / 폭발반경 150 / 폭발피해 28
- 로비 상세와 인게임 Run 상세는 동일한 `base_stats`를 기반으로 표시한다.
- 전투 중 현재 스탯은 `base_stats`에 마왕 레벨/일반증강/전역 배율을 적용해 계산한다.


### Galmuri UI 타이포그래피 규격 v2
- 프로젝트 뷰포트 1080x1920을 유지하며 Galmuri11 픽셀 폰트는 명시적 크기를 6px 단위로 스냅한다.
- 기본 규격:
  - 본문/보조 정보: 24
  - 일반 버튼/탭: 30
  - 섹션/몬스터 이름: 36
  - 큰 HUD/강조 제목: 기존 계층을 유지하되 6의 배수로 스냅
- Main/Lobby 씬의 명시적 폰트 크기와 런타임 생성 UI 폰트 크기를 동일 규격으로 맞춘다.
- 목적은 PC 0.5배 디버그 창과 다양한 모바일 해상도에서 픽셀 폰트의 비정수 축소로 인한 흐림을 줄이는 것이다.

### 인게임 몬스터 상세정보 패널 v2
- 책갈피 명칭은 `도감` 대신 `상세정보`를 사용한다.
- 버튼 표기는 `◀ 상세정보`.
- 패널은 전체 화면을 덮지 않고 실제 전투 영역 내부에만 표시한다.
  - 상단: y=290 (상단 HUD 아래)
  - 하단: y=1424 (마왕 필살기 패널 시작 직전)
  - 우측 고정 폭 약 485px
- 패널이 열려도 전투는 계속 진행되며, 닫을 때까지 전투 HUD의 상/하단 영역은 가리지 않는다.
- 슬라이드 이동거리는 패널 폭에 맞춰 500px로 사용해 완전히 화면 밖에서 들어오도록 한다.


### 인게임 상세정보 패널 v3
- 상세정보 패널은 상단 HUD 아래부터 마왕 필살기 패널 시작 직전까지만 고정 높이로 사용한다.
- PanelContainer가 내부 최소 높이에 의해 화면 아래로 늘어나는 문제를 막기 위해 내부를 ScrollContainer 구조로 변경한다.
- 상세정보 본문은 Galmuri11 기준 24px, 탭/버튼/섹션 제목은 30px, 몬스터 이름/패널 제목은 36px을 기본으로 사용한다.
- 내용이 많아지면 패널 자체가 늘어나지 않고 내부 스크롤만 발생한다.


### Run 결과 분석 강화 v1
- RunMetrics에 몬스터별 마왕 EXP 기여량과 첫 특수증강 획득 시각을 기록한다.
- 직접 소환 횟수/지휘력 소모 기록은 기존 RunMetrics 데이터를 유지한다.
- 전투 종료 분석에는 다음을 추가한다:
  - 마왕 최종 레벨
  - 몬스터별 마왕 EXP 기여량
  - 첫 특수증강 획득 시각
  - 일반증강 최종 레벨 목록
  - 특수증강 최종 획득 목록
- 결과 텍스트가 길어질 수 있으므로 결과창의 Run 분석 영역은 고정 높이 ScrollContainer로 표시한다.
- 특수증강 전투 시각 이펙트는 별도 리소스 준비 후 후순위로 진행한다.


### 돌연변이 전투력 재밸런싱 v1
- Stage 1 시간대별 돌연변이는 일반 몬스터보다 확실한 파워 스파이크를 만들도록 강화한다.
- mutation_1 (90초): HP x2.8 / 공격 x1.65 / 이동 x1.12 / 공격속도 x1.12
- mutation_2 (180초): HP x3.4 / 공격 x1.85 / 이동 x1.15 / 공격속도 x1.18
- greater_mutation (240초): HP x5.2 / 공격 x2.25 / 이동 x1.18 / 공격속도 x1.25
- 공격속도 배율은 attack_cooldown을 배율로 나누는 방식으로 적용한다.
- 폭탄쥐 돌연변이는 self_destruct_fuse에도 동일 공격속도 배율을 적용한다.
- 돌연변이 배율은 기존 마왕 레벨/일반증강/영구 연구 보정 위에 중첩된다.
- 폭탄쥐 신규 소환 자폭 피해/준비시간도 몬스터별 공격력/공격속도 증강과 마왕 레벨 보정을 정상 반영하도록 정리한다.
- 로비 엘리트 상세는 mutation_1 기준 공격속도 배율과 변경된 공격 간격/자폭 준비시간을 표시한다.


### 마왕 일반증강 빌드 집중 가중치 v1
- 일반증강 후보는 완전 균등 셔플이 아니라 이미 투자한 빌드를 이어가기 쉬운 소프트 가중치 추첨을 사용한다.
- 동일 증강 현재 레벨 기준:
  - Lv.1~3: weight x1.10
  - Lv.4~7: weight x1.20
  - Lv.8 이상: weight x1.30
- 몬스터 전용 증강은 해당 몬스터 계열에 이미 1레벨 이상 투자한 증강이 있으면 추가 weight x1.10을 적용한다.
- MAX 레벨 증강은 기존처럼 후보에서 제외한다.
- 동일 증강 ID는 후보 풀에 한 번만 존재하므로 한 선택창 내 동일 카드 중복은 허용하지 않는다.
- 일반증강 한 선택창에서 같은 몬스터 전용 카드가 3장을 모두 차지하지 않도록 기본 최대 2장으로 제한한다. 다른 선택지가 전혀 없을 때만 3번째를 허용한다.
- 목적은 빌드 집중을 보상하되 선택지가 완전히 고정되는 것을 막는 것이다.

### 마왕 증강 카드 몬스터 아이콘 v1
- 몬스터 전용 일반증강과 특수증강 카드에는 해당 몬스터의 MonsterCatalog.card_icon_path 도트를 표시한다.
- 공용 마왕/지휘력/성장 증강은 특정 몬스터 아이콘을 표시하지 않는다.
- 증강 카드 아이콘은 최대 폭 72px로 제한하고 원본 픽셀 리소스를 사용한다.


### Stage 2 Hero - 그림자 도적 v1
- Stage 2 hero_id는 기존 `swift_hunter`를 유지하지만 전투 정체성은 `rogue_combo` 근접 도적으로 교체한다.
- 로비 초상화는 `res://assets/art/heroes/stage2_rogue/stage2_hero_portrait.png`를 사용한다.
- 본체 도트는 `stage2_rogue/frames/frame_01~23.png`를 manifest 순서대로 사용한다:
  - idle 1~4 / move 5~10 / attack 11~16 / hit 17~19 / death 20~23
- 기본 평타는 3단 찌르기:
  - 1→2타 0.22초 / 2→3타 0.24초 / 3타 후 1.05초
  - 피해 배율 0.75 / 0.85 / 1.10
  - 매 타 약진 62, 넉백 42
  - effect_01 사용
- 1스킬 `난도질`:
  - 반경 205 내 몬스터 3마리 이상이면 자동 사용
  - 1.2초 동안 0.24초 간격 다단히트, 타격당 공격력 0.50배
  - 최대 HP 18% 쉴드 획득
  - 시전 중 이동속도 48%
  - effect_03 사용
- 노란 게이지 100% 스킬 `급습-암살`:
  - 0.35초 시전 후 투명 페이드
  - 반경 520 내 대상을 추적해 뒤쪽으로 순간이동하며 최대 5회 빠르게 공격
  - 공격 간격 0.18초, 피해 0.85배
  - 일반 몬스터 HP 30% 이하 처형
  - 돌연변이/대돌연변이/보스는 처형하지 않고 피해 x1.75
  - effect_02 사용
- Stage 2 도적 전용 임시 증강:
  - 예리한 연격 / 연격 가속 / 난도질 방어 / 끈질긴 급습 / 처형 감각
  - 기존 pursuit / slow_resistance / battle_recovery 일부도 함께 사용
- Stage 1 ranged_rookie 전투 루프는 기존대로 유지하고 rogue_combo일 때만 별도 상태머신을 사용한다.


### 용사 도트 화면 크기 표준 v1
- 모든 용사의 화면상 도트 크기는 Stage 1 견습 마도사를 기준으로 통일한다.
- 기준값은 Stage 1 첫 idle 프레임의 투명 여백을 제외한 실제 도트 높이에 기존 렌더 스케일 3.0을 곱한 값이다.
- 신규 용사는 원본 PNG 캔버스 크기와 관계없이 첫 idle 프레임의 실제 사용 영역 높이를 측정해 기준 높이에 자동 정규화한다.
- 따라서 64x64, 229x229, 512x512 등 서로 다른 원본 규격을 사용해도 인게임에서는 동일한 캐릭터 체급으로 보이게 한다.
- 캐릭터별 임의 scale 하드코딩보다 `_apply_normalized_hero_visual_scale()`을 우선 사용한다.


### Stage 2 그림자 도적 밸런스 v2
- Stage 1 견습 마도사보다 높은 근접 위험을 감수하는 만큼 기본 화력을 상향한다.
- 기본 HP 380 / 이동속도 270 / 공격력 36.
- 3단 찌르기:
  - 간격 0.20 / 0.22 / 0.90초
  - 피해 배율 0.85 / 1.00 / 1.35
  - 약진 70 / 넉백 30
  - Stage 1 초반 슬라임은 보통 2타 내 정리되는 수준을 기준으로 한다.
- 난도질:
  - 타격당 공격력 0.75배
  - 최대 HP 22% 쉴드
  - effect_03 스케일은 0.52
  - 캐릭터보다 위 z_index에서 재생해 몸을 일부 가리는 연출을 허용한다.
- 급습-암살:
  - 타격당 공격력 1.05배
  - 돌연변이/보스 추가 피해 배율 1.90


### Stage 2 그림자 도적 생존 증강 v1
- Stage 2 로비 초상화는 `stage2_hero_portrait2.png`를 사용한다.
- 도적 증강 풀에서 공용 `전투 회복`을 제거하고 전용 `피의 갈증`으로 교체한다.
- 피의 갈증은 최대 Lv.3:
  - Lv.1: 실제 직접 피해의 3%
  - Lv.2: 5%
  - Lv.3: 7%
- 평타 3단 찌르기와 급습-암살은 피흡 효율 100%를 적용한다.
- 난도질은 다단 광역기이므로 피흡 효율 50%를 적용한다.
- 피흡은 공격 전후 대상의 실제 HP 감소량을 기준으로 계산한다. 처형/과잉 피해도 대상의 남은 HP 이상으로 피흡하지 않는다.
- 소수점 회복량은 버퍼에 누적해 1HP 이상이 될 때 실제 회복한다.
- Stage 1 견습 마도사의 공용 전투 회복 증강은 기존대로 유지한다.


### 용사 리소스 공통 규격 문서
- 루트의 `HERO_RESOURCE_STANDARD.txt`를 용사 인게임 도트/로비 초상화 크기 기준 문서로 사용한다.
- 인게임 도트는 Stage 1 견습 마도사의 실제 idle 도트 높이를 기준으로 자동 정규화한다.
- 로비 초상화는 Stage 1 견습 마도사 초상화의 실제 사용 영역 높이/중심/캔버스를 기준으로 자동 정규화한다.
- 신규 용사 추가 시 개별 scale/TextureRect 눈대중 보정보다 위 공통 규칙을 우선한다.
- 스테이지 큰 제목은 침입 순서(`첫 번째 침입자`, `두 번째 침입자`)를 사용하고, 용사 직업/개체 이름은 별도 HeroName으로 표시한다.


### Stage 2 그림자 도적 밸런스 v3
- 7분 Stage 2에서 초반 슬라임 단독 압박에도 약 2분 내 사망하는 문제가 확인되어 생존 성장 구조를 추가한다.
- Stage 2 기본 스탯:
  - HP 440
  - 공격력 38
  - 이동속도 270
- Stage 2 전용 레벨 성장:
  - 레벨당 최대 HP +30
  - 레벨당 공격력 +3
  - 레벨업 시 현재 최대 HP의 18% 회복
- 위 성장값은 Hero profile의 `level_growth` 데이터로 관리하며 Stage 1에는 기본적으로 적용하지 않는다.
- Lv.7 기준 증강 외 기본 성장만 계산하면 최대 HP 620 / 공격력 56이 된다.
- 난도질 생존 보정:
  - 쿨타임 14.0초 → 10.5초
  - 쉴드 최대 HP 22% → 25%
- 목표는 Stage 2가 7분짜리인 만큼 초반 일반 슬라임만으로 너무 빠르게 붕괴하지 않도록 하고, 후반 연구/마왕증강 누적에는 여전히 압박받도록 하는 것이다.


### Stage 2 그림자 도적 밸런스 v4
- 레벨업 시 최대 HP 증가와 즉시 체력 회복은 제거한다.
- Stage 2 도적의 레벨 성장에는 공격력 +3 / Lv만 유지한다.
- 생존은 레벨업 자체가 아니라 난도질 쉴드와 피의 갈증 피흡으로 해결한다.
- 이유: 레벨업 HP 증가 + 회복 + 쉴드 + 피흡이 중첩되면 피해가 사실상 누적되지 않는 문제가 발생한다.

### 인게임 용사정보 드로어 v1
- 기존 몬스터 상세정보 책갈피 바로 아래에 붉은색 `용사정보` 책갈피를 둔다.
- 용사정보/몬스터 상세정보는 동시에 열지 않고 서로 전환식으로 사용한다.
- 드로어를 열어도 전투는 일시정지하지 않는다.
- 표시 정보:
  - Stage 1 기준으로 규격화된 용사 초상화
  - 현재 레벨 / HP / 공격력 / 이동속도 / 공격 간격 / 공격 사거리
  - 직업별 핵심 능력치
  - 현재 용사 증강과 중첩 수
  - AI 관측 주기와 최근 공세 기록
- 기존 하단 `용사 빌드` 라벨은 숨기고 빌드 정보는 용사정보 드로어에서만 확인한다.
- 전투 중 증강을 획득하면 열린 용사정보 드로어는 즉시 갱신한다.


### 전투 하단 HUD 정보 분리 v1
- 하단 마왕 스킬/소환 영역은 플레이어가 직접 조작하는 마왕 정보만 표시한다.
- 표시 유지:
  - 몬스터 소환 성공/실패
  - 자동/수동 배치 상태
  - 마왕 필살기 상태/발동 결과
  - 마왕 증강 획득
  - 돌연변이 및 스테이지 이벤트 안내
- 표시 제거:
  - 용사 레벨업 안내
  - 용사 증강 후보 목록
  - 용사 AI 선택 증강
  - 용사 AI 선택 이유
- 용사의 현재 레벨/스탯/빌드/AI 관련 정보는 상단 HUD 및 `용사정보` 드로어에서 확인한다.
- 용사 증강 선택 로직 자체는 기존대로 AI가 백그라운드에서 수행한다.


### 마왕 필살기 임계값 즉시 갱신 규칙
- 마왕 필살기 게이지가 최대치에 도달하는 프레임에는 주기 UI emit 타이머와 무관하게 즉시 `demon_ultimate_changed`를 emit한다.
- 마왕 필살기 개별 쿨타임이 0에 도달하는 프레임에도 즉시 `demon_ultimate_cooldowns_changed`를 emit한다.
- 이유: 임계값 도달 후 값이 더 이상 변하지 않으면 정기 emit 조건을 다시 타지 않아 버튼 활성화가 다음 소환/다른 스킬 이벤트까지 지연될 수 있기 때문이다.


### Stage 2 그림자 도적 범위공격 v1
- 도적의 모든 공격은 단일 대상 전용이 아니라 범위공격 기반으로 처리한다.
- 3단 찌르기:
  - 각 타격은 전방 타격점을 중심으로 원형 범위 피해를 준다.
  - 범위 반경: 1타 95 / 2타 110 / 3타 130
  - 전방 오프셋: 48 / 54 / 62
  - 범위 내 모든 몬스터가 동일 기본 피해를 받으며 넉백도 적용된다.
  - 메인 타겟 피흡 효율 100%, 추가 타겟 피흡 효율 50%.
- 난도질:
  - 기존 원형 광역 다단히트 유지.
  - 피흡 효율 50%.
- 급습-암살:
  - 메인 타겟 공격 후 대상 주변 반경 120에 추가 범위 피해.
  - 주변 몬스터 피해는 기본 암살 피해의 78%.
  - 메인 타겟만 일반 몬스터 처형 판정을 받는다.
  - 주변 몬스터에는 처형 판정을 적용하지 않는다.
  - 메인 타겟 피흡 효율 100%, 주변 타겟 피흡 효율 50%.
- 목적은 근접 용사가 다수 몬스터에게 포위되는 리스크를 HP/회복 수치로 덮지 않고 공격 구조로 해결하는 것이다.


### Stage 2 이벤트 타임라인 v1
- Stage 2 러닝타임: 420초.
- 기존 Stage 1에서 사용 중인 elite / miniboss / boss 이벤트 처리 구조를 그대로 재사용한다.
- 105초: 1차 추격 돌연변이
  - team selection
  - mutation_1
- 210초: 2차 추격 돌연변이
  - team selection
  - mutation_2
- 300초: 대돌연변이 추격자
  - team selection
  - greater_mutation
- 360초: 최종 침공 대장
  - orc 기반 boss
  - HP x6.5
  - 공격력 x2.30
  - 이동속도 x1.15
  - EXP x3.2
  - visual scale x1.45
- 최종 60초는 보스와 기존 일반 몬스터가 함께 남아 도적의 범위 전투 성능을 시험하는 구간으로 설계한다.
- 이벤트 자체가 일반 몬스터를 무료로 추가 생성하는 별도 웨이브는 넣지 않는다. 플레이어가 만든 군세와 이벤트 개체가 자연스럽게 겹치도록 한다.


### 상세정보 드로어 모바일 UX v1
- 몬스터 상세정보 / 용사정보 패널 모두 상단 `제목 + 닫기` 헤더를 스크롤 영역 밖에 고정한다.
- 본문만 스크롤되며, 아래로 내려도 닫기 버튼은 항상 보인다.
- 패널 바깥 영역을 터치/클릭하면 해당 상세정보 패널을 닫는다.
- ESC / 뒤로가기 입력 시 열린 상세정보 패널을 우선 닫고, 그 다음에만 전투 메뉴를 연다.
- 패널 내부 터치/스크롤은 닫힘으로 처리하지 않는다.


### Stage 2 엘리트 동반 군세 v1
- Stage 2 엘리트/대돌연변이/보스 이벤트는 특수 개체 1마리만 출현하지 않고 일반 군세를 함께 동반한다.
- 1차 돌연변이: 일반 군세 6마리.
- 2차 돌연변이: 일반 군세 8마리.
- 대돌연변이: 일반 군세 10마리.
- 최종 침공 대장: 일반 군세 12마리.
- 동반 군세는 현재 편성 몬스터 풀에서 순환 선택한다.
- 모바일 프레임 스파이크를 줄이기 위해 한 프레임에 전부 생성하지 않고 2마리씩 0.12초 간격으로 분할 생성한다.
- Stage 1 이벤트에는 적용하지 않는다.


### Stage 2 그림자 도적 공격력 성장 제거
- 기본 공격력: 38 -> 32.
- `level_growth.attack_damage_per_level` 제거.
- 레벨업 자체로 공격력이 증가하지 않는다.
- 후반 화력 성장은 용사 증강과 전투 빌드에서 확보하는 방향으로 유지한다.
- 3단 찌르기 / 난도질 / 급습-암살의 기존 피해 배율과 범위 구조는 유지한다.


### Stage 2 도적 / 슬라임 / 거미 밸런스 조정
- 그림자 도적 기본 공격력은 32 -> 38로 롤백한다.
- 레벨당 공격력 성장은 삭제된 상태를 유지한다.
- 도적 3단 찌르기 약진:
  - lunge_distance 70 -> 85
  - lunge_stop_distance 24 추가
  - 기존 고정 38px 정지 규칙을 데이터 기반으로 변경해 실제 약진 체감을 강화한다.
- 슬라임 기본 스탯:
  - HP 60 -> 72
  - 이동속도 115 -> 122
  - 공격력/비용은 유지
- 거미 특수증강 `삼중 거미줄`:
  - 기존 같은 방향 3연속 발사를 제거한다.
  - 용사 방향 기준 -18 / 0 / +18도 부채꼴 3갈래를 동시에 발사한다.
  - 각 투사체 피해 배율은 1.0.
  - 연속발용 burst 타이머 상태를 제거해 런타임 로직을 단순화한다.


### Stage 2 그림자 도적 불도저 콤보
- 3단 평타는 타겟 추적형 약진이 아니라 공격 시작 방향 고정형 돌파 공격으로 처리한다.
- 1타 시작 순간의 공격 방향을 콤보 방향으로 저장하며, 2타/3타도 같은 방향으로 전진한다.
- 각 타격마다 lunge_distance 85만큼 전진한다.
- 타격 판정은 시작점부터 전진 종료점+공격 오프셋까지의 선분형 통로로 계산해, 진행 경로의 몬스터를 모두 타격한다.
- 넉백은 방사형이 아니라 도적의 진행 방향으로 적용해 군세를 밀어내는 느낌을 강화한다.
- 공격 중 0.28초 동안 collision_mask를 0으로 내려 몬스터 물리 충돌을 무시하고, 종료 후 원래 mask를 복구한다.
- 3타 종료 후 다음 콤보 시작 때 새 방향을 다시 잡는다.


### 로비 UI 테두리 스타일 v1
- 로비 UI 외곽선은 이미지 장식이 아니라 Godot StyleBoxFlat 기반 공통 스타일로 유지한다.
- SafeArea/Layout/Content에는 비상호작용 ContentFrame을 두어 모든 탭이 같은 외곽 프레임을 공유한다.
- Header / StageCard / PortraitFrame / Shop Result / BottomNav / Team Card / Research Button / Monster Detail은 공통 색상·테두리 계열을 사용한다.
- ContentFrame은 mouse_filter=IGNORE이므로 탭 입력을 가로채지 않는다.
- 목적은 모바일 해상도 및 이후 로비 구성 변경에도 테두리가 깨지지 않도록 하는 것이다.

### 앱 시작 화면 흐름 예정
- 최종 진입 흐름은 `Fake Loading -> Login -> Touch to Start -> Lobby` 순서로 구성한다.
- 로그인 화면에는 카카오 로그인과 구글 로그인을 지원할 예정이다.
- 실제 카카오/구글 SDK 연동 전에는 인증 로직과 UI를 분리한다.
- Lobby.tscn 자체에 로그인/로딩 상태를 억지로 합치지 않고, 별도 Boot/Entry 씬이 Lobby로 전환하는 구조를 우선한다.
- Touch to Start 화면은 로그인 성공 후 로비 진입 전 대기 화면으로 둔다.
