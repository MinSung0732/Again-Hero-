# AGENTS.md

이 저장소를 수정하는 AI/코딩 에이전트는 작업 전에 반드시 다음 파일을 읽는다.

1. `PROJECT_CONTEXT.md`
2. `README.md`
3. `docs/ROADMAP.md`
4. `CHANGELOG.md`

핵심 규칙:
- 게임명은 **「용사, 또 너야? / Again, Hero?」**.
- Godot 4.x + GDScript 기반 모바일 게임.
- 핵심은 **AI 용사의 빌드를 관찰 → 유도 → 카운터**하는 역뱀서라이크 구조.
- 핵심 재미 검증 전에 불필요하게 시스템/콘텐츠를 크게 확장하지 않는다.
- 중요한 설계 결정이 바뀌면 `PROJECT_CONTEXT.md`도 함께 갱신한다.

- 기능 추가, 패치, 개선, 제거, 밸런스 변경이 발생하면 작업 완료 전에 반드시 `CHANGELOG.md`를 갱신한다.


## 데이터 중심 설계 원칙
- 향후 몬스터 / Hero / 증강 / Stage / 연구가 계속 추가될 것을 전제로 설계한다.
- 콘텐츠별 ID를 여러 로직 파일의 `match/if`에 반복해서 박아 넣는 하드코딩을 피한다.
- 이름, 역할, 비용, AI 가중치, 최대 레벨 같은 **콘텐츠 값은 Catalog/Data 파일**에 둔다.
- 런타임 로직은 가능한 한 Catalog의 데이터를 순회하고 공통 계산기로 처리한다.
- 새 콘텐츠 1종 추가 때문에 서로 무관한 여러 시스템 파일을 동시에 수정해야 한다면 데이터 구조 분리를 우선 검토한다.
- 프로토타입 단계라 과도한 추상화는 피하되, 곧 반복될 것이 확실한 규칙은 데이터화한다.
